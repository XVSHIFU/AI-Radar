# 工程测量记录

测量针对本次项目。测试结果、返修提交、工具等待分别记录，不能据单次原型分数推断模型的普遍能力。

| 项目 | 当前记录 |
| --- | --- |
| 开始时间 | 2026-09-11T16:04:25Z（本地 2026-09-12 00:04:25） |
| 总耗时 | 任务仍在进行，结束时记录；包括工具等待，不等于模型有效计算时间 |
| 主负责人 | Astra：契约、独立浏览器/API 验收、集成与关键审查 |
| 后端负责人 | Sol：独立 worktree，提交经主负责人检查后合入 |
| 前端负责人 | Terra：独立 worktree；原型比较后持续负责正式四页 |
| 实际货币费用 | unknown；Pro 周额度百分比不能推算现金费用 |
| 用户返工时间 | unknown；没有用户提供的计时，不能填 0 |
| 额度 | 起始剩余 95%，最近检查剩余 92%；到 80% 即停 |
| 工具异常 | Sol 摄取写入 exec 曾停滞约 17 分钟；主负责人中断恢复，保留已写文件 |
| 最终费用账本 | 当前未调用付费提取/embedding/回答服务；这不表示 Codex 或运行资源成本为 0 |

## 验收证据

- 两个原型共享 DOM 检查：14/14，详见 prototype-browser-results.json。
- 正式四页浏览器：最近一轮 33/33；随后截图发现手机导航换行，新增检查与修复进行中。
- 独立 HTTP API：14/14，详见 api-smoke-results.json。
- 最近一次统一检查：后端 20/20、前端 12/12；静态检查与构建通过。后续采集改动须重新检查。
- 真实数据库、历史 gold、真实模型质量均未通过，不能与以上合成检查混成一个总通过率。

## 重试与返修

原型 Sol：类型检查失败 1 次，构建因同一错误失败 1 次，修复后通过；移动浏览器 CLI 参数重试 1 次。原型 Terra：首轮构建失败/重试 2/2，移动修订构建失败/重试 1/1。具体命令耗时见各自原型报告。

正式前端出现过“报告完成但实际代码未接入”的返修，主审以实际 diff 和浏览器结果为准：

| 检查点 | 被退回原因 | 实际后续修复 |
| --- | --- | --- |
| SSE 失败终态 | b930f4a 的说明声称修复，实际未改 sse.ts | 77109e9，增加实际 error→done 回归 |
| JSON 引用编号 | 70ed237 增加 helper，但页面仍重编号 | 31e5fba 实际调用 askView，浏览器保留 index=2 |
| 详情生命周期 | 90f357a 同时误删相关实体与无效来源说明 | 9c324b5 恢复 |
| 模式切换 | 9c324b5 的 computed 只读非响应 location | 09d42cb 改为读取 Vue route；浏览器验证实际切换数据 |
| 手机导航 | 页面无水平溢出，但导航文字逐字换行 | 截图发现，新增文本行数与 44px 目标断言，修复中 |

正式后端 cbf0111 的报告称已统一 Evidence 校验，主审复查发现实际仍只在 articles_for 校验，独立 evidence_for 路径仍缺校验；已退回原负责人。该项修复前仍开放，不能仅据交付说明关闭。

浏览器脚本首轮 23/27，四项失败源于脚本在 Vue 更新 disabled 状态前点击；补充 DOM 更新等待后 27/27。扩展长内容、非连续引用、故障注入和模式切换后 33/33。这些脚本重试与应用缺陷分开记录。

这里的返修条目是可核实示例，不是所有内部工具重试的精确总数。最终只报告有记录的次数；不补造完整成本或返工计时。

主审验证脚本返修：增加36题清单完整性检查时，字符串插入误匹配两处导致1次SyntaxError；精确修复缩进后24项结构检查通过，12项保持未实现。当前额度90%。手机导航及CSS循环变量已修复，浏览器扩展至35/35。

2026-09-12 02:05 本地时间阶段复验：统一运行器9/9步骤通过，后端36项、前端12项；显式过滤结构24项通过/12项待实现；实际HTTP14/14、浏览器36/36。随后transport切片在owner工作区46项通过，待主审边界补丁及最终统一复验。Evidence独立校验已在0b5cbb9实际接入。周额度最新89%。
