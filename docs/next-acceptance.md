# 后续验收顺序

2026-09-14 已迁移到用户提供的 Ubuntu。用户明确当前先完成数据库与采集，暂不接入模型。
本轮环境、命令、备份恢复与服务状态见 [Ubuntu 开发记录](ubuntu-development.md)，
真实数据库测试见 [数据库验证记录](db-live-validation.md)。

## 本轮已推进

- PostgreSQL 16 + pgvector 0.8.6 运行中，迁移到 0005、真实 readiness 和隔离恢复演练通过。
- 五源 RSS + 最新正文样本通过生产抓取路径；首轮 1,352 个链接由持久队列逐篇采集。
- 候选绑定不可变原文版本，发布时间保留；未接入模型，不发布标准事件。
- API、worker、每小时调度和前端由 Ubuntu 用户服务管理，SSH 断开后继续运行。

## 之后仍需完成

1. 确定模型供应商、API 与预算后，实现候选提取、标准事件发布、实际 usage 账本与预算结算；
   校验主张、摘录、重复事件身份。仅设置 API key 不会启用尚未实现的生成。
2. 完成中文全文检索、embedding profile/维度/revision、向量故障降级、RRF 与结果快照。
   普通列表目前不保证跨页写入下的严格快照。
3. 接入真实问答/SSE，验证 scope_total/coverage、引用白名单、无证据、模型失败、取消与费用。
4. 拿到历史原站 ID/冻结材料及旧站抓包后，补足历史 gold 与兼容验收；逐题绑定 36 个 P0 用例。
5. 完成至少 5 个完整来源和 100 个真实标准事件的质量验收，再安排公网生产部署。
   原文数量不能替代标准事件数量，开发环境备份演练不能替代生产灾备方案。

用户已取消周额度 80% 停止规则。各层检查不合并宣传为 P0 通过率。
