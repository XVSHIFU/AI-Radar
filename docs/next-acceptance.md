# 后续验收顺序

本文件列出当前工程之外仍需完成的工作，不代表这些能力已经存在。用户已确认暂不提供PG实例和历史gold，先完成合成回归与可启动工程。

1. 恢复公开域名的真实公网解析，再运行 scripts/validate-source-bodies.py。本机当前把候选域名解析到198.18/私有IPv6，生产抓取器拒绝连接。不能把原urllib RSS入口成功代替生产链路成功。
2. 提供PostgreSQL16+pgvector环境，应用0001–0003并验证升级/恢复；实测幂等键、并发claim、租约失效、失败恢复、跨源同URL、同URL版本更新及精确总数。现有离线SQL与模拟会话不替代这一步。
3. 在持久正文和候选上实现模型提取与标准事件发布：供应商适配、调用账本实际usage/价格快照、预算预留结算、摘录与主张校验、重复事件身份。当前kept保持0，配置API key本身不会启用尚未实现的生成。
4. 完成中文预分词/PG全文、embedding profile维度与revision、向量状态降级、RRF及完整集合/内容版本快照。普通列表当前不保证跨页写入下的严格快照。
5. 接入问答生成和真实SSE，验证完整scope_total/coverage、引用白名单、无证据与模型失败分型、取消及账本。当前SSE界面检查使用明确模拟流。
6. 拿到原站ID/冻结材料后替换历史验收缺口；逐题绑定36个P0用例，完成至少5个完整来源、100个真实标准事件及真实引用质量验收。旧API/SSE兼容仍需要原站抓包fixture。

启动与检查命令见README；通过数只以docs中的逐项JSON报告为准。不同层的检查不能相加宣传为P0通过率。继续工作前运行scripts/check-codex-quota.ps1，周剩余<=80%即停止所有模型工作。
