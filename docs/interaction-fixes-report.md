# 已知交互问题修复（2026-09-14）

本轮按用户五张截图修复现有工程，未更换首页、统计或助手的既有视觉方向。

- 日期：自定义日历内的起止字段改为普通 YYYY-MM-DD 文本输入，保留手输、预设、日期范围校验与应用/取消。修复前实测弹层内存在两个原生 date 输入；修复后为零，只保留共享日历。
- 抽屉：桌面独立遮罩接收 click 后退层，背景主内容及导航 inert 并锁定页面滚动。修复前点底层第二个事件会从 5900 跳到 5901；修复后同位置命中遮罩，只关闭抽屉。已有研究助手继续可用，移动继续原生 modal 保护。
- 时间线：年份静态，月和日均可折叠；日期行显示“14 日”，完整日期保留辅助名称，分页追加保留折叠状态。
- 会话：首次发送展开会话区并推动输入框下移（580ms）；后续发送将最新用户问题平滑贴顶（380ms）。旧轮仍保留在 DOM 和 IndexedDB，可上翻查看。使用尾部留白处理短回答；减少动态偏好直接定位。
- 键盘：Enter发送，Ctrl+Enter显式插入换行；中文输入法组合输入不会误发。真实键盘曾复现默认 Ctrl+Enter 不换行，修正后通过。
- 范围：研究助手标题旁增加紧凑范围入口，默认收起；范围变化时可展开采用新范围。新建采用当前页面范围，旧会话快照不被覆盖。
- Markdown：用户问题、助手正文与输入预览共享渲染器；textarea保留原文编辑，预览/继续编辑切换。支持标题、强调、列表、代码、表格和换行。关闭原始HTML及图片语法，只开放http(s)链接，外链带noopener noreferrer。

验证：
- 前端单元测试27/27及生产构建通过，包含Markdown内容与注入边界测试。
- scripts/check-interaction-fixes.ps1：11/11，涵盖日历、折叠、遮罩、动画、IME、次问贴顶、历史回看/恢复、助手共存、手机布局及真实Enter/Ctrl+Enter。
- 首发输入框桌面top从约253px，动画中间约542px，到最终约766px；第二问桌面与手机距会话区顶部均小于1px。
- 原有会话协议回归13/13：流式内容、取消、引用、长文本、错误和晚返回隔离保留。
- 六张桌面/手机截图一次集中检查，见.impeccable/review/interaction-fixes；遮挡和溢出检查通过。单次设计检测器结果[]。
- DESIGN.md及侧车同步这次明确改变的行为。以上是前端交互和合成协议验证，不代表真实模型或采集能力验收。
