disposition: fix

证据：实际查看用户提供的3张图片及 round-2 指定的12张截图；截图内容、顶端及桌面/手机尺寸有效。只读源码及现有行为记录，未运行浏览器或第二次 detector。无批准 comp、QUALITY BAR 卡或新品牌批准，本轮按 code-first 结构选择预览审查；capture-results JSON 未读，几何通过不代替视觉结论。角色文件中的 inline 降级前提不适用于本轮真实独立 reviewer。

## persistence

pass：PRODUCT.md、既有 DESIGN.md 与 docs/design-lab-plan.md 均存在。计划记录 seed16614eb9、5/2/3 顺序和用户选择前仅交付独立 HTML 的边界。主 DESIGN 是既有品牌参考，本轮未批准正式重设计，不要求重写。无 comp 阶段，因此不要求 state/spec/plate 或 comp 批准记录。审查只写本文件。

## fidelity

| 项目 | 判定 | 证据 |
| --- | --- | --- |
| TYPE | acceptable adaptation | 中文工作字体和深蓝标题沿用既有身份；不是新品牌显示字体提案。但 mobile-a/b 的 SVG 文字随整体缩小至难读，须修复。 |
| MATERIAL | match | 代码原生几何图表与平面阅读符合用户要求，无假材质、虚构插画或栅格资产义务。 |
| GROUND | acceptable adaptation | 明亮冷银蓝底与可选深色符合方向；旧白色浮起阅读卡已移除。用户只引用 OneSOC 信息结构，没有授权将其黑绿配色作为像素目标。 |
| 统计主体与问答次级 | match / contradicted | A 桌面统计主画布与可收侧栏成立；B 下方答案仍是普通文档流，未展示约定的向上展开；手机 A 的侧栏排在长事件列表之后，首屏无法感知问答位置。 |
| A 分类至日期流向 | match | 桌面清楚表达分类与日期关联，并明确不代表采集漏斗；手机节点及字过小。 |
| B 分类面积趋势 | acceptable adaptation | OneSOC 大画布被转译为真实样本的堆叠面积，没有照搬安全告警语义；底部输入位置成立，答案展开结构尚缺。 |
| C 日期×分类矩阵 | contradicted | desktop-c、mobile-c 和 desktop-c-context 均出现分类标签与数值穿插、行列失配。源码固定 repeat(14,42px)，但默认只生成7个日期，导致网格自动排布跨行。点击结果计数正确不能补救图表不可读。 |
| 当前条件与摘要一致 | contradicted | browser-results 的 answer_scope_stays_current 失败；renderAll 未清除或重算已展开答案，分类由20条缩为4条时旧摘要仍为20条。 |
| 动态平面与日期阅读 | match | 桌面/手机事件直接落在背景，静态年份、月份折叠和按日组织均可见；搜索、更多及抽屉由现有行为记录支持。 |
| 导航与用户选择 | match | A/B/C 同等按钮可切换，明暗比较和来源说明可达，未把任一方向宣布为正式版本。 |
| 来源与真实性 | match | 页面明确42条合成事件、确定性摘要及未调用AI；来源说明解释维护者的健康/重试/更新职责，列出候选来源且标注尚未接入或启用。 |
| 紧凑反馈 | match | 事件悬停改文字颜色，箭头提示阅读动作；没有旧下划线。 |

共同契约的主旨、既有身份、统计到回查的阅读顺序和 seed 顺序均可对应计划；首屏记忆测试在桌面 A/B 成立，C 因网格失配失败，手机 A/B 因图中文字失读而不能公平比较。

## ceiling

无 QUALITY BAR 卡，不发明新的品牌完成度目标。OneSOC 参考敢于让关系图成为整页焦点；当前 A/B 已给予图表空间，但手机仍把桌面图缩成微型图，C 则尚未形成可读统计画布。应完成现有三个结构的表达，不添加安全装饰、中心徽章、特效或虚构指标来填空。

## material_fixes

1. C／结构契约：按 activeDates 的实际列数生成网格，保留明确日期表头及6条分类行；手机使用局部横向滚动与可辨认行标签。默认7天、本月14天和自定义单日都必须保持一格对应一个日期×分类，选中格须有可见定位。
2. Truth／当前范围：日期、分类或图表焦点变化时，清除或重新计算 A/B/C 已展开的摘要及 C 上下文；任何“当前范围”文案不得继续显示旧集合。复验20→4条及单格→换范围。
3. B／候选差异：把答案实现为从底部提问区向上展开的有界面板，保留可见图表上下文和明确收起动作；desktop-b-answer 不能继续只是输入框后追加正文。同时给“查看摘要”稳定宽度，避免桌面按钮断为两行。
4. A/B／移动可读性：不要整体缩放 SVG 文字；为390px提供独立布局或可滚动画布，使分类、日期与数值至少保持可读的约12px，并减少目前图形上下的大段空白。A 提问入口须在图表附近可发现，展开区不要排在长事件列表之后；保留其与 B 底部提问的区别。

## keep

保留平面背景、统计主导、A/B/C 等权选择、同42条合成样本及真实性说明、动态按日阅读、来源说明和正式 UI 未改的交付边界。
