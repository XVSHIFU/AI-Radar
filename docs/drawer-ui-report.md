# 时间层级与叠层抽屉

用户于 2026-09-13 确认事件 → 来源 → 证据三层：桌面保留后层边缘，手机逐层覆盖并返回；年、月、日可各自折叠。沿用简洁的银蓝阅读空间，增加可操作的日期层级与阅读抽屉。实现开始约10:20（Asia/Shanghai）。

范围为 HomePage 与新增 timeline、EventDrawers 和专用样式；详情、问答、采集及后端接口保留。抽屉地址通过 event/source/evidence 表达层级，保留原有查询条件和详情直链。分组只统计已加载匹配事件，不把当前页数冒充完整年/月统计。真实数据库、模型与历史材料验收不在此次前端范围。

## 验证与返修记录

- 纯逻辑测试增加跨年跨月、未知日期及追加分页后的折叠状态保留；前端测试合计18项。
- 新浏览器回归覆盖日期折叠、分页追加、三层打开、返回、Esc、刷新恢复、焦点/滚动恢复、390/768/1440宽度、失效层级、延迟事件/证据与跨年月合成结构。
- 原42项回归的首页选择器更新为新事件类名。详情故障重试直接调用真实应用路由进入保留的详情页，新默认事件抽屉入口单独验收。
- 首次新回归3/23：旧类名和折叠后失效的DOM引用导致脚本误读。修正后15/23，部分历史导航跳出旧累计浏览器会话；新标签页重现单层返回正常，后续每次回归隔离标签页。不能将旧会话异常归为已证实的应用根因。
- 原生dialog在三层程序打开后，单次Esc实测触发三个不可取消的cancel事件并关闭三层；已交负责人以键盘捕获阶段只退回顶层修复。
- 架构核对要求证据返回后再次校验请求代次、不同来源不得按相同标题合并、初始深链接锁定背景滚动，以及打开/关闭保留原URL参数。
- 第一轮12张截图有效；集中修复抽屉高度与叠层边缘、阅读标题层级、合成提示、时间目录辨识度和首屏空间。

最终独立裁决见下文。截图仅保存在本地 .impeccable/review/drawers/；脚本与结果JSON可提交。现金费用、用户返工时间未知；周额度遵守80%停止线。

## 最终行为核验（2026-09-13）

- 新增抽屉与日历浏览器回归24/24，原四页回归42/42；各层分别记录，不合并成后端验收。
- 最终前端类型检查、18/18单元测试、生产构建通过。键盘捕获监听的注册与卸载均实际接线，Esc只关闭顶层。
- 主线程第二轮桌面1440与手机390截图共12张，涵盖首页首屏/全页、月份收起及三层抽屉；几何检查12/12。768宽度的交互与边界由浏览器回归覆盖。
- 本轮设计检测仅执行一次，结果为空数组，见drawer-design-detection.json；不替代独立视觉审查。
- 周额度监测修复为按最近更新的会话文件读取，支持会话跨日续写。最近2026-09-13 11:02观测98%；严格遵守小于等于80%停止线，不据此推算现金费用。

## 独立审查

首次独立裁决为fix，见drawer-finish-review.md；要求补足抽屉进入/返回动效、重新安置年份箭头、手机展开/折叠文字与44px操作区、隐藏后层边缘里的零碎正文与按钮。交由原前端负责人集中修复，复验由原审查者逐项裁决。

## 交付裁决

2026-09-13 11:19，原独立审查者实际检查同12张返修截图，将4项修复全部判定resolved，disposition: ship。该裁决仅覆盖这4项修复，不冒充整站或P0验收，见drawer-finish-verdict.md。普通/减少动态效果的真实浏览器检查2/2，进入和返回均验证位移或即时切换，以及后层内容隐藏与父层焦点恢复。最后CSS修复补足年份列宽，避免年份与箭头再次换行。

当前预览 http://127.0.0.1:5175/ ，后端仍为明确标识的合成演示数据。最终源码集成提交c798c16；约10:20至11:19完成实现、两轮主线程视觉检查、一轮独立审查返修和原审查者裁决，约59分钟（后续文档收尾另计）。设计记录由独立documenter核对并补充。没有生成或新增位图资产，没有部署或推送。

文档收尾于2026-09-13 11:22完成：DESIGN.md及.impeccable/design.json已由独立documenter按源码扩展，JSON解析与差异检查通过。既有色彩、字体与其余页面规则保留；桌面全局日期操作40px与既有44px目标的差异如实记录，未将其写成合规结论。最新周额度11:22观测97%，高于80%停止线。自约10:20起总历时约62分钟，精确工具重试总数、现金费用和用户返工时间unknown。
