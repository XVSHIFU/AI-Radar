# 全方案逐项审计（2026-09-15）

已完整读取父目录《AI-Radar-Implementation-Spec-v1.0.md》33节，以及实施续篇、验收矩阵、工作包、后续验收顺序和专用Agent方案。
历史会话UI已修复并在Ubuntu生效；本轮新增后端功能已在Ubuntu隔离环境通过完整验证，运行服务更新等待单独确认。
目前不能将“工程回归通过”表述为所有质量与生产发布目标均已验收。

## 用户确认的范围

- 本项目从零开发，没有旧站。旧站URL/API/SSE兼容、抓包、T01/E01历史原始样本：**不适用**，不再等待。
- 新系统的日期完整性、实体筛选、证据定位、流式协议仍需验证。
- 专用研究Agent、复杂指代与计划、逐主张语义复核：按用户要求留到下一阶段，见 [Agent方案](research-agent-plan.md)。
- 用户已允许本轮源码同步到 `xvsf@192.168.194.129:/home/xvsf/ai-radar`；该同步已完成。

## 33节规格核对

| 规格范围 | 本轮处理与当前证据 |
| --- | --- |
| 1–5 架构、依赖、环境 | PostgreSQL16+pgvector、API/worker/前端独立进程；Ubuntu继续作为开发环境；补Linux统一检查和CI定义 |
| 6–8 数据、日期、证据、索引 | 新增日期依据/冲突、冻结引文哈希与主张字段、归并日志/撤销；0008→0009→0010真实迁移链通过 |
| 9–12 查询、中文检索、RRF、快照 | 中文bigram全文/GIN、固定BGE CPU配置、硬过滤后的RRF、完整不可变分页快照；新查询在数据变更后得到新修订，旧游标保持原内容 |
| 13–16 来源、队列、恢复、归并 | 7个来源已有标准事件；持久任务保留失败/退避；补人工归并/撤销、旧ID解析、成员证据及实体统一读取、并发采集写入锁 |
| 17 模型与用量 | 生成实际usage记账，未知值不伪装0；BGE运行于本地CPU；新增15分钟增量索引维护脚本，不调用付费API |
| 18 API | 新项目OpenAPI已冻结；补管理员日期核查、归并历史/撤销、持久审计接口；旧站兼容不适用 |
| 19–20 SSE与证据 | 保留真实流式、引用白名单、逐字段落定位、断流/取消状态；主张语义独立核验不得与原文定位混为一谈 |
| 21 会话指代 | 浏览器历史保留；复杂指代与专用Agent按用户决定下一阶段 |
| 22 前端 | 历史面板样式祖先作用域错误已修复；桌面/平板/手机无溢出；新增后台数据核查页与日期口径说明 |
| 23–26 验收与质量 | 逐题绑定见 [验收绑定](acceptance-bindings.md)；工程、合成结构、真实PG、真实模型、性能分别报告；人工语义质量门槛仍需独立评估 |
| 27–29 阶段与交付 | 主要工程缺口已补齐并隔离验证；Agent另列，不根据页面数量或测试总数宣称全部阶段通过 |
| 30 CI、运行、恢复 | 新增CI工作流、每日备份与隔离恢复、5分钟健康报告、私网TLS发布候选；CI尚未在GitHub运行，未配置外发告警 |
| 31 性能 | 10k/20并发：完全冷列表462ms、连接池就绪后的首次快照240ms、同修订稳态73ms、关键词124ms；100k仅诊断，见 [性能报告](retrieval-performance.md) |
| 32–33 风险与交接 | 旧站外部依赖已取消；公网域名、独立备份目标尚未提供；维持私网开发入口与同机可恢复备份 |

## 本轮最终验证

- 历史面板故障前：行display=block，更多SVG=0×0；修复后flex，桌面SVG16px，手机18px/44px触控。1440/768/390检查通过，不改用户浏览器会话。
- 后台数据核查浏览器验收：1440/390无横向溢出；未确认原文不能改日期；请求绑定Evidence和CSRF；401清空私有界面并返回登录。
- 前端42项测试、类型检查、生产构建通过。
- Ubuntu最终后端：**271 passed，1 skipped**；跳过的是需单独启用的性能基准。包含真实PG完整升级/降级/再升级、真实BGE、归并/撤销并发、索引分批推进与旧版本计算结果拒绝。
- Ruff和mypy通过；固定业务日期/精确分页新增8项通过；独立确定性计划30/30、显式结构24/24、真实拒绝连接3/3。以上不是可相加的P0质量通过率。
- BGE已按上游CLS配置修正并更换profile版本；真实索引/激活/检索通过。固定模型文件在持久目录，SHA-256已校验，见 [模型验证](retrieval-validation.md)。
- 新事件向量索引在LIMIT前排除已就绪版本；提交前重新检查事件内容；事件版本变更通过数据库trigger使旧向量失效。短事务统一锁防止归并、日期核查、采集、索引激活互相覆盖。
- 升级前恢复演练：0007、8来源、1483报道、1485版本、293事件、1145条可定位Evidence，DeepSeek主体检索2条。只恢复新建临时库，未覆盖主库。

## 真实数据覆盖与验收边界

当前库有293条已发布事件：AWS128、arXiv69、NVIDIA50、Hugging Face24、Google Research12、Anthropic8、DeepSeek2。
OpenAI来源暂无产出，保留抓取受限与partial记录。7来源产出和293条入库记录不自动等于100条人工质量金标准。
既有事件日期保留为`report_date_unverified`待核查；不限日期时间线继续显示，严格日期统计排除未核验/冲突日期。

仍需单独完成的验收：人工标注语义Recall@20、真实事件去重/日期/主张样本质量，以及有实际域名和独立备份目标后的公网发布与异机灾备。
完全冷连接池列表462ms仍高于300ms示例目标；不得用稳态73ms代替冷启动结果宣称全部达标。

## 发布状态

本轮代码已提交Git并同步Ubuntu；隔离验收通过。现有运行服务仍使用升级前版本。
自动审批拒绝了“停止API、worker及定时器并合并发布”的动作，理由是继续开发授权未明确覆盖多服务停机；已向用户单独请求该步骤确认。
拒绝发生在执行之前，没有停掉运行服务。备份与恢复验证已完成；获准后再执行迁移、重启和上线核对。

运行和恢复命令见 [运行手册](operations-runbook.md)。
