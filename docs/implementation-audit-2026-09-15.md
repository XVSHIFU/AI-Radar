# 全方案逐项审计（2026-09-15）

本次完整读取父目录《AI-Radar-Implementation-Spec-v1.0.md》33节，以及实施续篇、验收矩阵、工作包、后续验收顺序和专用Agent方案。
以当前代码/数据库/测试证据核对，不把历史报告中的“通过”或“未实现”直接沿用为当前结论。
用户指定：先修复历史会话UI，专用Agent编排留到下一步；其余缺口继续实施。

## 审计发现与处理

| 规格范围 | 起始核查结果 | 本次处理 / 验收边界 |
| --- | --- | --- |
| 1–5 架构、依赖、环境 | PG16+vector、API/worker/前端独立运行；Ubuntu已取代初版Windows部署 | 保留用户指定Ubuntu；增加Linux统一检查和CI定义 |
| 6–8 数据、日期、证据、索引 | 冻结版本与逐字引文有效；事件日期仍沿用报道日，无检索索引/profile | 日期依据/冲突、主张字段、归并与撤销已本地实现；迁移0008待获批同步后完成真实PG集成 |
| 9–12 查询、中文检索、RRF、快照 | 确定性计划30题通过；普通查询明确不保证跨页内容快照 | 中文全文、BGE CPU向量/profile、RRF与全内容快照已实现；正在修正性能瓶颈，最终集成仍待迁移链 |
| 13–16 来源、持久任务、恢复、归并 | 8源配置，293已发布事件；源超时真实标partial；跨报道归并缺失 | 补可追溯合并/撤销与旧ID解析；数据数量不代替去重质量 |
| 17 模型、用量、预算 | 实际usage记账；未知费用不假装0 | 用户已明确不要求估算费用；保留unknown，有限并发/无自动计费重试不变 |
| 18 API | 新API真实可用；缺旧站冻结契约 | 新增功能随代码冻结OpenAPI；旧兼容等待用户材料 |
| 19–20 SSE、证据、答案状态 | 真实上游SSE已有实测；引用白名单/定位及断流草稿有效 | 不把逐字存在说成语义已独立验证；继续补可审核证据元数据 |
| 21 会话指代 | 浏览器历史持久化已交付；复杂指代尚未完成 | 按后续用户决策保留浏览器会话；专用Agent/F类留到下一步 |
| 22 前端 | 历史面板样式祖先选择器错误，更多SVG为0尺寸 | 已修复；1440/768/390无溢出，菜单/图标/44px移动触控验证 |
| 23–26 gold、36题、P0质量 | 24结构断言通过、12原题待扩展；旧T01/E01材料缺失；未完成人工质量门槛 | 分开报告合成/真实/性能数据，不能将233工程测试称为36题通过 |
| 27–29 阶段与交付清单 | UI/后台/SSE提前交付，数据检索仍存在P0/P1缺口 | 按上述依赖补齐；不按页面完整度宣称全方案完成 |
| 30 CI、运行、恢复 | 无CI工作流、定时备份或运行检查；旧恢复只核计数 | 新增CI、每日备份/隔离恢复、5分钟健康报告、TLS私网发布网关 |
| 31 性能 | 首次1万/20并发：列表p95 5.45s、关键词4.30s，未达目标 | 检索实现后使用独立performance fixture，不能污染线上事件库 |
| 32–33 风险、外部输入、交接 | 未提供原站gold/完整旧契约、公网域名、独立备份目标 | 明确保留外部验收项，不伪造、不擅自扩大公网暴露 |

## 已完成的本轮验证

- 历史面板修复前：行display=block，更多SVG=0×0；修复后display=flex，桌面SVG16px，手机18px/44px触控。
- 浏览器1440/768/390：历史标题省略、会话行、更多菜单无横向溢出。测试用独立浏览器本地会话，不改用户会话。
- 初始基线233个后端测试（含真实PG隔离库）通过；故障HTTP3/3、确定性计划30/30；前端42项与build通过。
- 私网TLS网关使用本地CA显式验证证书，首页200、health ready、未知API JSON404；未使用忽略证书错误参数。
- 恢复演练：0007，8来源/1483报道/1485版本/293事件/1145可定位Evidence，DeepSeek主体查询2条。只恢复新建临时库。
- 定时备份03:30、健康检查5分钟已经启用；当前健康报告提示1次采集partial，保留真实告警。

## 当前集成状态与新增验证

- 本地已合并历史面板和数据质量分支；新增后台数据核查入口：日期候选与原文对照、明确确认后修正、人工归并/撤销、脱敏操作记录。
- 浏览器合成接口验收：1440/390无横向溢出；未确认原文不能修正；POST绑定所选Evidence和CSRF；401后清空管理面板并返回登录。
- 本地前端构建与42项回归通过。日期口径补充后，Windows适用非PG测试221项通过；既有POSIX0600权限用例不适用于Windows，未修改该验收，仍须Linux执行。
- 严格日期范围排除未核验报道日期及冲突日期。不限日期的时间线仍保留这些旧记录并明确标注；数据库条数不代表已完成日期/语义质量验收。
- 管理员持久会话与审计0010已单独在临时PG验证DDL升级/降级；完整0007→0008→0009→0010链尚未验证。
- 自动审批拒绝了数据质量子任务向用户指定Ubuntu传输源码包；已向用户明确目的地、文件范围并请求批准。尚未收到答复，没有换传输路径绕过该拒绝。
- 检索首次10k性能不达标，正在优化全量ORM加载和快照读取；不能把功能通过当成性能通过。

最终集成证据通过后再补充。未部署或未通过的条目不计为全部完成。

## 明确保留项

1. 专用研究Agent及结构化指代/复杂计划：用户指定留到下一步。
2. 原站T01/E01 gold与完整旧API/SSE兼容：依赖原站地址/样本，已请求用户补充。
3. 公网TLS域名发布和独立故障域的备份：需要目标信息；本次只做私网发布候选与同机可恢复备份。
4. 真实语义Recall、100事件去重/日期/主张质量及性能门槛：需相应实现和具体评估证据，尚不能以库中293条或单元测试总数替代。

运行命令与来源依据见 [运行手册](operations-runbook.md)，Agent范围见 [专项方案](research-agent-plan.md)。
