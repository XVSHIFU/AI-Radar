# 统计主导的平面界面 · 设计候选预览
2026-09-14；用户要求先预览、选择后再修改正式页。本轮仅新增独立设计实验文件，不改生产路由、主DESIGN或现有业务代码。
## 用户输入
去掉AI动态浮起的大白卡，内容直接落在背景板；重新组织筛选与时间线。统计成为主体，参考用户OneSOC截图的信息层级，提供问答位置与交互的创意选择。去掉hover下划线，用紧凑文字颜色/轻底色/箭头反馈，键盘焦点仍保留。顺便检查导航、日期/计数/图表含义与管理入口。图表需动态、可交互。
## 概念范围
沿用公众阅读的银蓝身份做页面结构重排，附深色比较；正式品牌主题由用户选择后决定。模式Operate/Read；用户既定code-first。只做代码原生图表，不生成静态GIF来冒充统计。
自有候选顺序：1 日期日报+旁注；2 时间潮汐+底部提问；3 分类矩阵+就地提问；4 主题对照+双列解释；5 事件流向+右侧问答；6 分类环形+抽屉问答；7 可缩放事件地图+命令入口。
本轮surface seed16614eb9取5、2、3，按该顺序展示A/B/C，同等可选。没有选择完成，不宣布任一方案为正式替换。
## 共同契约
统计独立于AI；图表来自同一明确合成的事件数组，保留确切日期、数值、数据表和空态。流向只表达数据中存在的分类、日期或重要度关联，不编造真实来源流量/采集漏斗。时间播放有界可暂停，reduced-motion下静态。A侧栏/B底栏/C就地面板，都要能对当前选中范围给出明确标识的程序摘要，不伪称调用模型。
首页平面阅读、月份折叠、事件抽屉与紧凑筛选在三个方案共用。管理入口次级，公众可看来源说明，维护者管理操作仍受原权限约束。
## 研究与来源
- Apache ECharts官方数据过渡说明：https://echarts.apache.org/handbook/en/how-to/animation/transition/ 。适合后续正式图表的动画更新。
- Apache ECharts官方项目：https://github.com/apache/echarts 。本轮预览用原生SVG/JS，不引入依赖。
- Observable官方动态交互：https://observablehq.github.io/plot/features/interactions 。其当前页面说明不提供增量动画过渡，暂不作为本轮动效实现。
现有来源注册候选来自backend/src/radar/register_sources.py：Hugging Face博客、arXiv cs.AI、Google Research、AWS Machine Learning、NVIDIA Technical Blog。注册默认disabled/unverified，不代表真实启用或已采到。
后续扩充优先原始发布源、研究论文与官方GitHub Releases；社区热榜只作为发现线索，事件依据回到原始材料。当前真实PG与正文生产链路未验收，页面仍为合成数据。
## 交付与审批
独立URL /design-lab.html 是此次预览交付；serve-question三方案用于确认。正式页面保持26efe7f前后原状。用户选择后另行按选定方案实施。

## 首轮核验
原负责人首版057d708/f90c63e已集成为dfe1122/b9becd1。主线程实际查看桌面A/B/C与动态、手机A/B/C与动态共8张。发现合成分类公式恒为0、周/月按钮同范围、B仍是柱图、移动溢出、侧栏无法恢复、矩阵条件未同时生效和日期分组缺失；已集中交回负责人按原预览契约修正。未将首版称为完成或交付用户选择。
正式Vue/CSS/API保持不变，当前新增文件仅frontend/public/design-lab.html。图表和摘要明确标注为合成样本，不宣称真实采集或AI回答。

## 集成与核验
933e793为独立预览当前实现。第一轮8图后集中修正数据、布局及交互；第二轮12图实际查看，根停止视觉迭代，一次detector结果为空。独立fresh reviewer实际查看本轮用户3图及全部12图，提出4项：矩阵列错位、旧摘要、底部答案面板、移动可读性/问答入口。原负责人修复后同12路径重捕，几何12/12，交原reviewer按修复列表裁决。
浏览器首批11/12（旧摘要失败），修复后14/14：周20/月42、单日2、无效/空范围、矩阵双条件与7/14/1列数、问答侧栏恢复、范围变化清摘要、月份折叠/更多、Esc焦点、B底部面板/收起、播放暂停。node --check通过；独立静态HTML不要求重跑整个正式工程测试，不能声称生产业务链路已验收。
预览URL http://127.0.0.1:5175/design-lab.html 。结构选择页 http://127.0.0.1:51221/ ，key6e8b348c；用户未选，不将默认A当批准。周额度05:08Z观测83%，<=80停止；实际费用、用户返工时间和工具内部重试精确次数unknown。

同一reviewer第一裁决为fix：四项分别partial/resolved/partial/partial。剩余仅C持续选中定位、B可见收起、手机A默认紧凑提问入口；原负责人继续同一列表，未追加重设计。14/14行为通过不能替代这三项视觉/入口完成度。

## 用户在选择页的新确认（约13:20）
用户选择reroll并说明：内置agent不能仅回答统计页的问题，应在事件等每个页面随时打开，成为真正的全站助手，并询问更合适的设计。此输入覆盖原先各统计方案局部问答的假设。
新选择页用同一key6e8b348c更新，surface seed16614eb9/reroll1/candidate-count5，dealt4/5/3。候选空间从全站使用方式推导：1上下文指令中心、2会话时间轴工作区、3并排助手、4随行浮窗、5助手工作台；此次展示4/5/3。均为新的全站入口及会话生命周期，非原局部问答改名。
建议并排助手：全站固定入口；默认收起；右侧展开，当前内容仍可读；切换页面保留会话，明确显示当前绑定事件/日期范围/证据；支持继续固定原上下文或主动切换，不能悄悄换检索范围。手机用有明确返回/收起的面板。来源管理的修改操作仍受维护权限约束，公众助手只读取公开内容。
此轮交付是全站助手布局草图供选择，尚未实现真正跨页Agent。已有design-lab.html继续保留统计图和动态的交互草案，其局部问答不是全站助手最终实现。前一轮最终独立裁决3项resolved、B收起位置partial（按钮在滚动内容下方）；未将该草案宣布为完整通过。用户新范围优先，正式页面仍未替换。

选择页更新时工具报告旧key6e8b348c服务器已结束；按工具指示重新启动，全站助手选择URL现为 http://127.0.0.1:61184/ ，key2ec05531。最新候选payload保存为design-lab-global-choices.json。用户可在新页选择或用会话直接说明，未产生正式改版批准。
约12:47开始，13:24阶段交付；用户经选择页补充全站助手需求后，当前交付分为既有统计交互草案和全站助手布局草图，后续实施待用户选择。最终周额度05:23Z观测82%，未触发80%停止线。保留旧B面板收起位置partial，不将16/16行为检查、12/12几何或通过的三项修复扩大为全草案通过。
