## verdict

同一 reviewer 实际查看本次更新的全部12张同名截图，证据有效；仅复核原四项及上次列明的剩余，没有扩展审查或运行浏览器。行为记录16/16及390px入口记录通过。

1. **resolved — C 行列与选中定位。** desktop-c 和 mobile-c 保持正确日期列、分类行及可读文字；desktop-c-context 的09-09模型发布格有明确深色描边，与下方上下文对应，现有记录确认重绘后选中仍保留。
2. **resolved — 当前范围与摘要一致。** 保持上一轮解决结论；最新 answer_scope_current 通过，desktop-c-context 的选中条件和1条计数一致。
3. **partial — B 向上展开与收起。** 底部答案面板及完整按钮文字保持；收起动作已加入，现有记录证明关闭与焦点恢复。不过 desktop-b-answer 首次展开仍看不到收起按钮，源码把 sheetClose 放在五条事件列表之后、可滚动内容底部，未满足上一轮明确列出的“始终可见”要求。
4. **resolved — 手机图表与 A 入口。** mobile-a 现在默认只显示紧凑“展开问答侧栏”入口，统计数字和可读流向图回到其后，不再被整段问答挤压；mobile-b 仍保留可读刻度与底部提问结构，390px行为记录确认入口可展开。

## remaining

仅初审项3的可见收起尚未完成：将 B 的收起控件放在答案面板始终可见的头部或固定操作区，首次打开及内部滚动时无需寻找；保留现有点击关闭、Esc和焦点恢复。16/16行为通过不能覆盖截图中缺失的可见性。此次修复未观察到原审查范围内的新回归。

disposition: fix
