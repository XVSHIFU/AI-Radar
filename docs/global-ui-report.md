# 全站并排助手与正式统计视图
2026-09-14。本轮落实用户已批准的设计：AI 动态仍为首页，统计为独立页面，固定浅色；保留 A/B/C 并采用全站并排助手。实验 design-lab.html 未作为正式外壳。

## 实施状态
后端联合统计主提交 24615a5；最终前端源码 3d73e29。功能回归完成，独立审查第 1 次修复裁决三项均 resolved，结论 ship 仅限本轮 UI/fixture；设计文档按最终源码同步。
用户明确取消 80% 额度停止线并要求继续。08ee4b4 已更新简单安排.txt、检查脚本和本轮契约；不再因剩余额度触发停工。

## 数据与范围
summary.daily_categories 提供日期升序×固定六分类且补零，与总数、每日及分类边际一致，使用完整匹配结果。图表不调用模型。
A 为分类到日期的流向，B 为可播放堆叠趋势，C 为日期分类矩阵；共用聚合结果，保留数据表与图表筛选。流动动效表达关联，不表示实时采集。
助手沿用原问答协议，提交实际筛选并保留本次范围和演示/服务模式；跨页保留最近一轮回答。首次提问前范围自动跟随；提问后采用新范围需要显式操作。当前接口没有事件 ID 过滤，不将列表范围声称为单事件范围。维护令牌不进入助手请求。
真实 PostgreSQL、真实采集发布和生成模型仍未完成，页面使用明确标识的合成数据；模型未配置时不伪造 AI 回答。

## 可复验结果
- 后端负责人：pytest 107 通过，Ruff/mypy/OpenAPI 一致性通过；PG 仅 SQL 与事务合同，无真实实例。
- 独立 HTTP：12/12，含日期分类每项计数、空日期、组合筛选与日期边界。
- 前端负责人：20 项单元测试通过；最终换行修复后生产构建通过（含类型检查）。
- 原问答协议：12/12，覆盖 JSON/SSE、取消、引用编号、错误与查询计划。
- 统计与全站助手：14/14，覆盖联合总数、三视图、分页、图表下钻、跨页范围及 token 隔离。
- 首页与抽屉：19/19，最终 CSS 修复后复跑通过。
- 手机助手与层级关闭：6/6，含长回答可达返回、背景 inert/滚动锁及 Esc 仅关顶层。
- 动效：3/3，验证流线 dashoffset 变化/暂停、面积路径变化/停止、加载时减少动态效果禁用两类播放。
- 独立审查定向行为：7/7，真实四处悬停无下划线，A/B/C 图表聚焦后可按 ArrowRight 横向滚动。
不同层与模拟协议响应不能相加当作真实产品链路验收。JSON 文件记录各层实际执行时刻；未声称所有测试都在最终机械 CSS 提交后重跑。

## 视觉证据和限制
根首轮 12 图，集中修正后确认 14 图（1440 桌面、390 手机和用户 1680 宽），均实际查看且无整页横溢出。图表区域允许内部滚动；full-page 截图中 fixed 手机面板只占首视口，其后的底页不是实际滚动能力证据。
一次 detector 记录 global-design-detection.json：Inter 为沿用身份；新增 padding-right 动画已删除。未重跑检测。
根两轮后停止自行视觉打磨；独立审查提出三项有界修复：B 图例对比、手机图表横向浏览提示与键盘操作、旧链接下划线。四张定向复拍已实际查看，另有 7 项真实交互检查。最终裁决记录在 global-finish-verdict.md。

## 验证过程记录
协议迁移最初 11/12，实际计划模板补齐后曾因 Vite 返回旧组件仍显示失败；用 HTTP 内容与磁盘源码对比确认并重启服务，最终 12/12。统计脚本切范围时曾过早读取卸载图表，修正等待后 14/14。手机背景检查改为实际 main 与 rail inert 边界；真实 Esc 级联关闭另行修复后 6/6。
收尾删除下划线时错误写入字面量 PowerShell 换行符，导致 CSS 编译失败；3d73e29 修正为真实换行，搜索无残留，生产构建通过并重启预览。七项定向检查最初 6/7：浏览器 hover 命令未将视口外统计链接滚入视口，补实际 scrollIntoView 后仍断言真实 :hover，最终 7/7。未降低无下划线或键盘滚动断言。
本轮约 13:50 开始，中间按旧规则暂停并在用户取消后继续；于 2026-09-14 15:03:38 +08:00 完成独立裁决、文档核验与交付记录。内部工具重试精确总数、实际现金费用和用户返工时间 unknown。
