## verdict

证据检查通过：已逐一查看 .impeccable/review/reviewer-fix-1/ 的全部 10 张必需截图，内容、顶部和尺寸有效。本轮仅评分既有四项修复。

1. **resolved — 问答条件布局。** ask-desktop.png 的“研究条件”独立占行，分类及日期有完整可读宽度；ask-mobile.png 中条件位于提交按钮之前，已不需越过回答修改范围。
2. **resolved — 问答引用锚点。** 两张问答截图的展开引用均显示“段落 demo-p-001 · 来源版本未提供”，来源标题、摘录和模拟标识仍清楚。
3. **partial — 首页首屏密度。** 桌面事件容器起点已上移至约 480px，首条摘要完整；但日期范围仍断为两行、清除仍独占一行。mobile-viewport.png 首条事件容器约 580px，正文标题约 640px，事件尚未占据首屏主要面积；只缩留白没有完成要求的条件操作重排。
4. **unresolved — 日期对齐。** desktop.png 和 desktop-viewport.png 的日期仍显示为“2026-09-”与“12”两行。加宽日期列的规则位于 max-width:900px 中，因此没有修复桌面；移动截图却承受 108px 日期组左内边距，正文仅余约 190px 宽。

本批引入的回归：**移动首页阅读列被日期槽挤窄。** 相比 round-2，mobile-viewport.png 的日期和正文大幅向右移动，首条摘要增加为多行，降低了阅读密度；该回归属于第 4 项修复本身。

## remaining

- **第 4 项最小修复：** 把 92px 日期宽度、108px 日期组内边距及相应时间线位置限制在桌面断点；移动端明确恢复日期组 padding-left:0、日期标题 width:auto / text-align:left、节点 left:0。桌面日期加入 nowrap 和 tabular-nums。证明：同视口 desktop.png 的所有日期单行；mobile-viewport.png 恢复靠左、宽阔的阅读列，完整日期仍可读。
- **第 3 项最小修复：** 将移动“分类与日期”和“清除”合为一行操作，并压缩日期组前的剩余无信息间距，使首条事件开始于 500px 之前；不要缩小正文字号或触摸目标。桌面将完整起止日期安排在同一范围行，并把清除并入操作。证明：两张 1000px 高 viewport 截图中事件区超过半屏、首条摘要完整，日期与条件没有意外换行。
- 第 1、2 项已通过，下一批保持即可。最终仍需在相同 1440×1000 / 390×1000 视口提供全部 10 张有效截图，复核上述剩余项及修复引入的回归。

disposition: fix
