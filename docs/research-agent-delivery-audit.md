> **2026-09-19 内容生产增补**：默认手动复制/导入与可选内容 Agent 已上线并通过生产只读核验；写入闭环在隔离库验证，自动模式默认关闭，不混用公众助手配置或额度。参考资料已受控整理，错误实体隔离，来源种子默认禁用。当前状态见 [内容生产整合方案](content-pipeline-plan.md)，旧方案保留。

# 专用研究 Agent 交付核对（2026-09-16）

> **当前上线状态（2026-09-16，优先于下文历史记录）**：用户已明确要求上线新 Agent、改为 Docker 并只保存同机副本。现已完成：pi Agent 与受限 gVisor Python 启用、全栈容器切换、0012/0013 迁移、原数据及配额保留、实际数据库导出/恢复、一份镜像/数据库/配置备份。公开入口 48 小时 5 次，付费提取暂停。独立存储和大规模付费质量评测不再是当前交付前置条件；原质量指标未冒称已达到。当前运行路径、验收与恢复说明见 [个人容器部署](personal-container-deployment.md)。下文各阶段“尚未上线”“待迁移”“P5 未完成”均为之前时间点的记录。


最新用户决定（2026-09-16）：优先个人开源项目可用性与费用控制，避免重复无效测试；不默认执行大批量收费质量评测。公开助手已改为同 IP 48 小时 5 次，隐藏额度行；自动付费提取已按用户要求暂停。旧版门槛记载保留作历史，当前范围及线上证据见 [费用控制调整](cost-control-20260916.md)。

原方案与 V2 均保留。本表区分源码完成、候选环境验证和线上启用；整个 P0–P5 目标尚未完成。

| 要求 | 当前证据与状态 | 剩余工作 |
| --- | --- | --- |
| 后端选型 | 固定版本 pi-agent-core 候选运行时；供应商仍由后台配置 | 完整部署与回退验收 |
| 文档、Skills、权限规范 | `agent/research/` SYSTEM/TOOLS/MEMORY、固定技能和校验策略已建立 | 后续变更仍需评测，不能加载任意路径或安装插件 |
| 问答、只读查询、数据分析 | 范围冻结、工具代理、调用与 token 上限、Python 接线已有测试 | 实际公开入口→pi→PostgreSQL→gVisor→文件下载联合验收 |
| 禁止宿主操作和越权 | 模型无 shell、Docker、管理员工具或供应商密钥；独立可信控制器持 Docker 权限 | 最终 Compose 网络、挂载和凭证边界验收 |
| 同 IP 48 小时 20 次问题 | P1 已上线，追问计数、共享入口、持久账本、可信 IP 与幂等 | P5 恢复后账本和窗口不能重置 |
| 匿名用户隔离 | 签名匿名会话；IP 只限额；数据集绑定当前 run，文件绑定 owner/run | 联合验收跨用户与恢复场景 |
| 有限 Agent 编排 | 最多三次模型、四次业务工具、一次 Python；默认禁止 Python | 公开部署开关仍关闭 |
| 供应商兼容 | 已批准的 DeepSeek 验证完成两次调用，记录见 P2 | 原受限授权已使用；不得据此增加收费调用 |
| Python 隔离 | 固定 runsc/image、无网、非 root、只读、资源上限，15 项真机用例通过 | 新生产入口已通过 Ubuntu 健康与合成分析验收；公开入口联合验收待完成 |
| 取消、超时、崩溃清理 | 独立 watchdog 真机验证；私有 HTTP 断连清理通过 | 单实例锁与退出释放已通过真机验证；启动遗留故障演练和 Compose 生命周期待完成 |
| Python 产物与图表依据 | JSON/CSV/PNG、限量与格式校验、授权下载、到期；分析来源含代码和输入数据 | 生产全链路与 P4 图表事实检查 |
| 助手展示 | 候选前端支持主题化图表、下载、过期提示、展开依据，中英文与手机检查 | 已同步并通过 Ubuntu 构建；仍为候选，尚未切换线上 |
| 流式与引用 | 真 SSE、草稿与终态区分、正式来源结构校验和失败门禁 | 结构有效不等于语义正确；P4 人工语义核验仍缺 |
| 40 问冻结质量基线 | 原要求保留：任务完成率≥90%、定位有效率100%、语义抽查≥95% | 40 问合成样本已冻结并通过离线校验；真实旧/新答案对比、人工标注、失败回归及计量报告尚未完成 |
| 记忆 | 本地已完成有界提取式摘要、逐次明确授权发送、偏好查看/保存/删除和清除边界；详见 P4 记录 | 已同步 Ubuntu，回归与构建通过；完整质量基线与最终发布待完成 |
| 部署隔离 | gVisor 已安装，独立控制器/公开 API/模型工具的代码边界已建立 | P5 全栈分离 Compose；不能声称“整个项目已隔离部署” |
| 备份、恢复、切换 | 原服务已有备份基础；新 Agent 尚未切换 | 新全栈独立备份与恢复、无双 worker、无配额重置、回退演练 |
| 数据保留 | 候选 0013 清理迁移和隔离库测试已完成 | 线上仍为 0011；0012/0013 正式迁移待 P5 |

## 本地新增：控制器健康门槛

`sandbox_controller.py` 是仅供操作员启动的单进程入口。仅监听 loopback，凭证来自操作员私有文件，无公开 API 配置或数据库加载。进程锁避免同一主机重复控制器；没有将 Docker 权限交给模型。

启动核对固定 runsc 二进制摘要、root 所有权与不可被普通用户写入的路径、Docker runtime 配置和固定本地镜像。只清理符合既有所有权和过期条件的遗留容器；尚年轻的任务留给独立 watchdog，并拒绝启动就绪。不会猜测其他任务可以删除。

运行中每五秒检查独立 watchdog、runtime、镜像与执行器清理状态。健康证明超过十二秒失效；任一检查失败保持拒绝，需操作员排查后重启。认证的 `/health` 与实际执行共享门槛，公开 API 在扣问题额度前查询健康状态。

当前入口使用宿主 systemd watchdog，**不是最终 Compose 的 watchdog 方案**。Compose 必须提供独立、可验证的清理进程状态，不能伪造健康字段。

本次本地 `test_research_*`、`test_sandbox_*`、`test_python_preflight_quota.py` 共 171 项通过；真实 pi 子进程测试开启，模型上游为夹具。四个受影响后端模块按 Linux 平台严格 mypy 通过，Ruff 通过。测试集合与之前的 191 项切片不同，不将两者相加计数。此结果不能替代 Ubuntu 实际入口验收。

## 同步与当前线上状态

Ubuntu 候选目录目前仍在上一轮已同步状态，本次 Python 接线提交 `e52b338` 和记录修正 `012b514` 尚未传输。自动审批拒绝了此次新源码同步，认为既有授权只覆盖之前特定补丁；已向用户提出包含具体目的地和源码范围的确认，未绕过拒绝。

只读复核显示 runsc 摘要正确，运行时路径各级由 root 所有、权限 755，watchdog timer active、最近任务成功，任务标签下无遗留容器。该复核没有修改远端代码、服务或数据库。

公开 Python、新 Agent 开关和线上迁移均未因本次本地开发而改变。完整交付目标继续保持未完成。


## 后续验收更新

用户明确批准 `e52b338` 与记录修正后，二者已同步并应用到 Ubuntu 候选目录，远端提交为 `a310bd6` / `a8aa5a9`。上方“尚未传输”为此前时间点，不再代表这两个补丁的当前状态。

Ubuntu 结果：pi 15 项、研究/沙箱/扣费前后端 159 项、前端 50 项与生产构建通过。真实网关复验报告 `/home/xvsf/ai-radar/.run/sandbox-gateway-20260916-02.json` 通过：3 个分析文件，分析 1,416 ms，断连清理 103 ms，两个容器均删除；跨用户下载、外来数据集、重放拒绝；模型调用 0。

隔离 PostgreSQL 的 25 项通过。首次错误地用候选模块加载配置时提示 Database not configured；没有数据库操作发生。随后自动审批要求证明数据库隔离，经核对远端/本地 fixture blob `d93f447cede804c9f974739f6e0e2bd001465d92` 一致，以及随机 radar_test_ 建库、专用 Harness URL、强制 loopback 和清理前缀检查后，重新获准执行。线上库未迁移，原服务仍 active，无遗留沙箱容器。

`f0db5a4` 的启动健康入口与后续会话记忆代码仍仅在本地；本次授权的同步没有混入这些新改动。[P4 进展](research-agent-p4-progress.md) 记录记忆实现和仍缺的质量评测。


## 第二批获批同步与验收（2026-09-16）

用户随后明确批准 `f0db5a4` 和 `6c798ac`。二者已依次应用到同一 Ubuntu 候选目录，远端提交为 `ef0f555` / `c01ce38`，工作区干净。因此上一段“仅在本地”属于此前时间点。

- Ubuntu 后端研究、沙箱、问答、公共入口及扣费前检查 233 项通过（启用真实 pi 子进程测试，上游为夹具）；pi 运行时 15 项通过。
- 前端 57 项通过，类型检查及生产构建通过；七个受影响后端模块严格 mypy 与 Ruff 通过。
- 复用已核准的随机 `radar_test_` 隔离 PostgreSQL 测试器，25 项通过；未迁移线上库。
- 真实执行 `python -m radar.sandbox_controller`，临时私有凭证仅在远端生成并清理，监听 loopback。无凭证健康请求返回 401；认证后确认固定 gVisor、镜像与独立 watchdog 就绪。
- 实际 Linux 进程锁拒绝第二个持有者；执行合成数据 `2+3` 得到 5 和 `sum.json`，耗时 229 ms，任务容器已清理。相同任务重放返回 409。经过一次监控周期仍健康，结束本次控制器后可重新取得锁。
- 真机报告：远端 `/home/xvsf/ai-radar/.run/production-controller-20260916.json`，本地 `.run/production-controller-20260916.json`。本轮模型调用为零。

验收后 API、前端、worker 与 watchdog timer 均 active，无残留沙箱任务。没有切换线上服务、启用公开 Python 或执行正式数据库迁移。本次证明生产控制器入口可运行，不等于公开 API→pi→PostgreSQL→gVisor→文件下载的完整联合验收；P4 质量评测和 P5 隔离部署、备份恢复仍未完成。


## 后续本地进展：完整链路测试与冻结质量基线

新增 `backend/tests/integration/test_research_python_e2e.py`（提交 `17a9c8f`）覆盖正式问答函数与查询规划、pi、隔离 PostgreSQL、真实生产控制器和 gVisor、JSON/CSV/PNG、分析来源、SSE/JSON 两种响应、跨用户拒绝、过期、追问和幂等额度，以及控制器不可用时的扣额前拒绝。只替换付费供应商为夹具；Python 只在临时策略副本及 loopback 测试进程启用，原策略不修改。Windows 完成静态检查和用例收集；没有 gVisor 时明确跳过，不能据此称为真机通过。

本次新测试补丁的同步被自动审批拒绝：此前逐批授权未覆盖 `17a9c8f` 的源码外传。没有通过改名、远程写入或其他执行方式绕过。需要用户明确批准这个新增测试补丁后继续 Ubuntu 联合验收；原线上服务未受影响。

四十题合成基线现已冻结，摘要 `57cd0133455918a8bb26e367469e39fe3c2b9a3e1da3b54c5c2000c1bc6af5e6`。12 条事件、14 段证据、实体别名、明确日期区间、实际请求模板和异常／记忆操作步骤均已准备。离线评分器验证原数值门槛、缺失评审、安全关键项、引用和语义分别计量、旧新同条件及未知用量。12 项回归通过，空白实际评分为 incomplete。

这不等于真实模型质量验收：双路径运行器、实际答案采集、浏览器操作采集和人工复核仍缺。详见 `agent/research/evals/QUALITY.md`。这些本地质量文件没有混入已批准或被拒绝的远端补丁。本轮没有模型费用。


## 本地样本装载与采集进展

新增隔离空库装载、单次流式采集、只读 owner/request 账本读取三个评测部件。新增 25 项回归通过，与先前评分器共 37 项；完整语料真实仓储用例已准备但未执行。它们不会自动启用模型、连接业务库或更改公开额度。旧／新批量编排、浏览器操作、实际质量对比和 P5 部署恢复仍未完成。

`17a9c8f` 的远端同步授权仍待回复。本次只推进本地独立工作，没有重试被拒绝的传输或间接远程写入。


## 本地 P5 进展：容器独立 watchdog 与凭证边界

已补容器控制器入口、独立清理进程的认证健康证明和关闭/失败处理；主机与容器入口统一使用同一文件描述符检查、读取私有凭证，拒绝符号链接和阻塞管道。新代码没有登记为模型工具。

watchdog/健康/响应边界回归 39 项通过；14 项 Linux 文件边界用例在 Windows 明确跳过。四模块 Linux 严格 mypy 与 Ruff 通过。此结果不代表容器编排或 Linux 文件权限已真机通过。

本轮没有同步新增代码、收费调用、服务切换或正式迁移。完整编排、独立备份/恢复、单一活动 worker 和额度保留仍待实现与演练，详见 [P5 进展与接线边界](research-agent-p5-progress.md)。


## 本地 P5 进展：核心服务候选编排

新增十一项服务的候选 Compose、固定摘要构建文件、分离数据库身份、逐服务文件凭证和同宿主共享进程锁。默认、active 与全部组通过官方 Compose CLI 配置解析；部署边界与控制器回归 51 项通过，16 项 Linux 专属测试跳过，五模块类型检查及 Ruff 通过。

这不是容器启动成功记录。真实构建/初始化/网络/锁与恢复验收仍缺，历史扫描、提取和向量索引尚待接入；线上、公开 Python 与模型费用均未改变。详见 [候选部署说明](../deploy/containers/README.md) 和 [P5 进展](research-agent-p5-progress.md)。


## 后续本地进展：历史维护与语义检索配置

历史扫描、后台模型事件提取、增量向量索引及首次激活已接入独立候选配置组。历史缓存有专属可写卷，模型制品只读且启动时验证固定摘要；API 的 embedding 覆盖配置补齐语义查询依赖。监督进程在失败/超时/供应商中止时退出，不自动重试，取消时清理进程组后才释放锁。

测试复现并修复了“子进程创建期间取消会漏清理”的竞态，重复取消与直接子进程退出后的残余组也有逻辑回归。维护/部署/控制器测试 63 项通过，16 项 Linux 专属项跳过，三模块类型检查及 Ruff 通过；官方 Compose CLI 验证全部十五服务和 embedding 合并配置。Ubuntu 仅只读核对了既有公开 tokenizer 的 SHA-256/文件大小，没有同步源码、启动维护、调用模型或改线上。

这些是接线与本地验证，尚不等于容器运行验收。真实构建、数据库和模型加载、信号清理、权限/网络隔离、独立备份与恢复、配额保留和无双 worker 仍待完成。详见 [候选部署说明](../deploy/containers/README.md)。


## 本地恢复包校验进展（2026-09-16）

已实现操作员专用恢复包封装与只读完整性检查，拒绝覆盖、遗漏、额外文件、路径穿越和摘要不一致；原始 HMAC 密钥按字节保存。17 项合成制品测试、Ruff 与两文件严格 mypy 通过。这不是数据库恢复记录，真实捕获、独立恢复、额度保留和无双 worker 验收仍未完成。

只读复核 Ubuntu 候选目录仍为获批的 `c01ce38` / `ef0f555`，工作区干净；本批新源码未传输。主机只发现系统文件系统和安装光盘，独立备份位置待补充。后续恢复必须处理快照后新增账本，不能从旧备份直接重新发放额度。详见 [恢复包与恢复验收](research-agent-recovery.md)。


## 一致性快照后续进展与存储确认

用户已确认暂时没有独立备份存储，先继续本地恢复工具，P5 独立恢复门槛保留。新增只读 repeatable-read 快照与六表逐行指纹比较，固定序列化/主键顺序并保留 pg_dump 可导入的快照上下文。相关测试合计 25 项通过，1 项真实 PostgreSQL 并发写入验收因本地无实例而跳过，静态检查通过。未执行实际 dump/restore，未传输新源码、调用付费模型或更改线上服务。详见 [恢复包与恢复验收](research-agent-recovery.md)。
