# 专用 Agent P2：候选运行时与可信模型网关

2026-09-16。继承 [专项增补方案](research-agent-v2.md)；本记录不是完整 Agent 上线验收。

## 已实现的候选代码

- `agent/runtime/` 使用实际安装的 `@earendil-works/pi-agent-core` 0.85.1 和自定义模型流，直接与间接依赖锁定；安装禁用脚本。独立 Node 服务每轮创建新 Agent，显式注册七个研究工具，不注册 shell、文件操作、插件发现或 Python。Node 不持有模型供应商密钥。
- 运行时 HTTP 接口限制身份凭据、输入体积、并发、截止时间和客户端断连；默认仅监听回环地址。真实 pi 循环按串行工具执行，上限三次模型调用、四次业务工具、两次 Skill，取消不自动重试。
- `research_guard.py` 在 Python 侧独立实施一次运行的随机能力令牌、截止时间、调用序号、预算预留与结算，以及模型签发的工具调用 ID/原参数匹配。重启后能力令牌失效，不能由客户端恢复。
- `research_stream.py` 新增受限工具流适配器，保留原普通问答适配器禁止工具的行为。按 SSE 帧读取，正文立即输出，工具参数分片只有在完整结束后才组装；拒绝重复 JSON 键、非有限数值、非法调用 ID、重复调用、过大响应、缺失结束标志。截断参数不可执行；缺失 usage 保持未知，不伪造零。取消关闭上游，无自动重试。
- `research_gateway.py` 提供可信会话核心，系统消息、用户已接纳问题、工具 Schema、模型回复和工具执行结果由后端构造，不接收 pi 自造的对话或权限。无效工具调用生成受控错误并在后端记录，避免 pi 本地拒绝后下一轮一直等待未执行工具。记账接口为必需依赖；接纳失败不调用模型，结算失败撤销权限。

协议依据：供应商支持 SSE 内容分片与工具调用，usage 可能随结束内容块返回；适配器也覆盖独立 usage 块。参考 [DeepSeek Chat Completions 文档](https://api-docs.deepseek.com/api/create-chat-completion/)。这里只实现并测试了协议解析，**没有以文档存在代替真实模型兼容验收**。

## 本地验证

- 实际 pi 核心：11 项通过。包括多轮依赖工具、首字实时输出、串行执行、工具与模型预算、取消、HTTP 客户端断连、运行间不复用上下文、凭据和内部思考不出现在公开事件中。
- Python 新增测试：36 项通过（权限守卫 6、模型工具流 19、可信网关 11）。加原普通模型流回归共 47 项通过。
- 三个新增 Python 源文件 Ruff 和严格 mypy 通过；Node TypeScript 检查通过。
- 所有供应商响应及执行器均为测试夹具；测试使用实际 pi 与真实 HTTP 流边界，但没有调用收费模型，没有执行 Python 沙箱代码。

## 本轮继续完成：范围工具、内部 HTTP 与多次调用账本

- `research_tools.py` 实现七个受限研究工具。每轮使用独立的 PostgreSQL `REPEATABLE READ READ ONLY` 事务，查询超时 10 秒、空闲事务超时 95 秒，运行权限继续受 90 秒截止限制。检索返回分页条数与完整范围总数；统计直接聚合授权范围，不从分页推算。日期统计排除未核验、未知和冲突日期；跨期比较处理零基期、不同天数、区间重叠和未知采集缺口。
- 证据来自本轮快照中的文章版本，必须定位到段落并通过已有 quote hash 校验。事件 ID、游标、数据集只在原运行和范围内可用，取消或退出后销毁临时索引。图表仅引用本轮数据库数据集；Skill 由服务端从固定规范包加载，没有文件路径或安装接口。
- `research_http.py` 实现内部模型/工具回调路由和限量活跃会话注册。检查随机能力令牌、严格请求字段与体积；pi 附带的 context 不进入供应商请求，后端自行维护真实系统规则、历史和工具结果。该路由尚未挂入线上应用，后续公开网关必须继续禁止访问 `/internal/`。
- `research_ledger.py` 与迁移 `0012_research_model_calls` 为每次付费模型调用持久化序号及输入/输出预留，并复用 `llm_calls` 记录实际用量，供应商费用未知时不编造金额。数据库锁独立拦截重复调用、上一调用未结束、次数与累计预算超限。公开问题结算现改为在父记录锁内汇总子调用；取消与新调用竞争时不会释放已经接纳的调用费用。此结算变更仍只在候选源码中。
- Ubuntu 的 Node 22.22.1 未编译原生 TypeScript 支持。运行时现先通过 TypeScript 编译，再执行 `dist/src/server.js`，不依赖可选的类型剥离功能，也未改动宿主 Node 版本。

### 新增执行证据

- Ubuntu 独立测试数据库：19 项通过。包括范围工具 5 项、多次调用账本 5 项、原公开配额 8 项、完整迁移升降重升 1 项。测试数据库由夹具创建并清理，未迁移或写入运行中的业务库。
- 范围快照测试在另一连接实际修改事件、来源标题并撤下事件；本轮仍读取原值和原总数，下一轮看到新数据。只读测试实际提交 UPDATE 并收到数据库拒绝；跨会话数据集、游标和范围外事件访问被拒绝。
- 账本测试创建新的服务实例读取已有调用，验证未知用量保持预留、重复调用只入账一次、取消后的迟到结算不退回先前保守扣减，以及父记录结算与新模型接纳竞争时的费用一致性。
- Windows 与 Ubuntu 均通过编译后的 11 项 pi 测试及 3 项内部 HTTP 测试。其中端到端测试启动真实 Node pi 进程和 Python HTTP 服务，经模型回调、工具回调、第二轮模型回调完成回答；供应商响应与工具结果是测试夹具，不是收费模型质量验收。
- 本地相关核心与 HTTP/schema 测试 42 项通过；新增工具、HTTP、账本及公开结算模块类型检查通过。

### 当前部署状态

候选补丁已同步至已授权 Ubuntu 的 `/home/xvsf/ai-radar/.run/research-validation` 隔离工作树，依赖按锁文件安装且禁用安装脚本。当前运行服务仍使用原公开助手和 `0011_public_ask_quota`，**没有启用专用 Agent，也没有升级线上数据库到 0012**。没有调用真实收费模型。

## 尚未完成的接线与验收

1. 创建公开研究运行控制器：固定规范加载、准入后的范围事务、内部会话注册、模型配置及安全 HTTP transport、多次调用账本与 pi 客户端装配；挂接内部路由与有开关的公开入口。
2. 完成公开 SSE 适配、草稿轮次替换、最终引用核验、取消与剩余额度联动，再对后台实际配置模型验证工具参数流、usage 和引用。数据库工具与模拟供应商的分别通过，不等于真实模型全链路通过。
3. 运行时机器策略与规范包一致性检查；目前 `policy.json` 仍标记设计合同，不能写成已被运行时加载生效。关键词工具尚未复用既有混合检索，质量阶段需要验证召回覆盖，不掩盖降级。
4. P1 补充保留期治理：窗口外 IP HMAC 清理与幂等记录独立保留期尚需实现。20/48h 配额本身已经上线，但清理策略不能遗漏。
5. P3 Python 隔离、P4 原 40 问及增补质量基线、P5 Compose/独立恢复仍未完成。

完整专用 Agent 目标保持未完成；下一切片是公开运行控制器与 SSE/引用接入。


## 公开接入候选代码（2026-09-16，尚未启用线上开关）

- 新增公开研究控制器和部署开关，连接已接纳的问题、只读范围事务、七个工具、内部能力会话、pi HTTP 客户端、安全供应商 transport 与持久化模型调用账本；普通和流式入口共享配额与结束结算，旧助手路径保留。
- SSE v2 用轮次重置事件替换上一轮草稿，防止工具前的文字混入最终答案；失败输出受控终态，不能记作成功。空范围直接返回无匹配事件，不发起付费模型调用。没有依据的事实回答被替换为说明缺口的提示。
- 证据和统计数据集均由服务器分配本轮数字引用，最终答案拒绝未知编号、UUID 引用和自行生成的 URL。数据库数据集保留类型、范围快照、数量及日期排除口径，不伪装为原始文章证据。结构核验不等于语义事实核验，后者仍属于 P4。
- 固定规范包由 Python 和 Node 各自检查权限、工具白名单、预算、只读记忆规则与禁用 Python；两端 policy/SYSTEM 摘要必须一致，漂移时在问题准入前拒绝。`policy.json` 改为候选运行时约束状态，不代表已部署或 Python 已启用。
- 前端沿用 A 布局/E 时间线，以可横向滚动的本地化表格展示统计依据。agent-browser 检查了 1440px 桌面、390px 窄屏及六类合成统计数据，表格未撑宽页面；截图仅用于界面验证，不是实际模型研究结果。
- 本地新增公开接入测试 27 项、pi 测试 13 项通过；前端 47 项测试和生产构建通过，相关后端 Ruff/严格 mypy 通过。新增真实 pi + 公开 HTTP + PostgreSQL + 记账集成测试，准备在 Ubuntu 独立测试库执行，收费供应商仍用夹具。

前文待办 1、2、3 已有候选实现，但还缺公开全链路/真实供应商兼容验收及上线步骤。P1 保留期治理、P3 Python 沙箱、P4 质量基线、P5 Docker/恢复保持未完成。
