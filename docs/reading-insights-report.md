# 月份阅读与规则统计总览
本轮约2026-09-14 10:54开始。用户先确认首页/抽屉方案，再补充总览应由程序按日积累整合、不依赖AI，默认一天并支持周/月/自定义与常用筛选。继续沿用Terra前端、Sol后端、根验收的分工。

## 范围
- 原版 / 与 /preview：年/日静态，月份折叠；按需清除，鼠标与键盘焦点区别；完整事件在抽屉、证据直接呈现，来源信息按需展开。
- 事件/来源：外遮罩或Esc关闭最上层，内部操作不误关；保留轻量返回；子层在右、父层左退，手机覆盖。
- /ask 与 /preview/ask：公众总览默认今天，近7天、本月、自定义最多366天；分类、关键词、重要度4+共用筛选；完整集合统计、图表下钻、分页明细及同范围提问。
- 总览由确定性代码与数据库聚合，不调用AI。按问题筛选复用/query-plan规则解析并显式应用；真实生成模型仍未配置，不声称已上线自主Agent或AI制图。
- 日期按Asia/Shanghai、含起止，未知日期不进入限定时间；统计只代表库内事件，不代表全网覆盖或真实采集发布已经完成。

## 接口与兼容
新 /api/v1/insights/summary 接受date_from/date_to/q/category/min_importance，完整匹配集合统计，每日含0、固定6类含0。Postgres数据库聚合置于只读repeatable-read事务；没有真实PG环境，仅SQL合同验证。
原/api/v1/insights的headlines响应与原stats保留。根早期误以为insights路径可新增，经源码审查发现已有接口，已由原负责人恢复旧接口并分离summary路径。既有LLM、抓取和依赖配置不扩展。

## 已知返修与验收记录
- 首批源码报告遗漏遮罩接线、预览按钮及旧focus规则，根指出后原负责人补齐。
- 新遮罩判断曾将内部键盘click的零坐标误判为外部，已限定event.target===currentTarget后再核对位置。
- 首轮浏览器检查8/9为实际鼠标焦点框残留；修复后脚本仍用outlineWidth判定，实际outlineStyle已为none，脚本按实际绘制样式更正。
- 抽屉测试曾取第一个:modal而非最上层，以及在手机进入动画完成前测量，分别修正选择器与等待；不将这些脚本误判记为产品缺陷。
- 问答重组曾省略既有SSE/计划字段，根要求恢复原实现；图表0值、月桶、独立失败与旧请求守卫按同批问题修复并复验。
- 根独立HTTP12/12通过，原insights返回global/3条headlines；Sol最终后端105项测试通过、Ruff/mypy/OpenAPI通过（后续错误协议调整另9项定向通过）。各层不相加。
首轮完整新浏览器35/35、既有问答协议12/12通过；14张截图几何14/14并全部实际查看。截图发现空态过长、轻量返回换行、版本字段跨端点比较产生假警告，已集中交回负责人修正。后续裁决与完成时间待最终验收填写。

## 限制
当前服务为fixture合成数据。今日无事件如实显示；不能悄悄切到有数据日期。没有真实PG迁移/事务或模型质量验收。新增统计从已入库事件查询，不是本轮已经完成真实每日采集/发布；生产规模的物化日报和调度可后续按需做。
现金费用、用户返工时间与精确内部工具重试次数unknown。周额度最新11:26观测88%，<=80%停止。

另记脚本修正：一处PowerShell字符串内引号造成JS语法错误；fixture近7天模型发布实际4条，早期手算5条已用HTTP核对修正；问句按钮点击须等待Vue更新一次。现有数据版本字段不构成跨请求一致快照，不能因不同端点token格式不同即判数据变化。

## 最终集成与验证
最终界面源码 d7be027。主线程第二轮14张截图已全部实际查看，几何14/14；首轮问题集中修正后结束主线程视觉迭代。文档源码核查发现意外本地路径文本、重复日柱规则、CSS变量仅置于兄弟元素以及填充width冲突，由原负责人修复，局部记录已复核。

- 新交互浏览器35/35；原问答协议12/12（2026-09-14 11:58）。最终前端单元18/18、类型及生产构建通过。
- 独立HTTP统计12/12，后端105项由Sol完成；没有真实PG服务，不能以SQL合同或fixture代替真实落库。
- 一次设计检测仅报告既有Inter字体使用警告，按保留现有设计世界处理，交独立审查判断。
- 第二轮截图与几何记录见 `.impeccable/review/reading-insights/round-2/`、`reading-insights-capture-results.json`；本轮35与12项记录单独保存，不以此前旧四页42项冒充本轮完整复验。
- 验收脚本最后一条旧文案断言已改为实际筛选值断言。一次误用Windows PowerShell5读取无BOM UTF-8造成中文JS语法错误，改用项目PowerShell7后35项通过；这是测试运行环境错误，不是产品回归。

默认今日遵守用户要求；合成库今日无事件，近7天25条、9月完整32条。统计代表匹配库内事件，而非全网事件；生产每日入库和日报物化调度不在本轮完成范围。静态data_revision不保证跨请求快照，移除跨端点token不等造成的假变更警告。

预览： http://127.0.0.1:5175/ask ，首页 http://127.0.0.1:5175/ 。独立设计裁决见本文末尾与reading-insights-finish-verdict.md。

## 独立审查修复
完整独立审查见 reading-insights-finish-review.md，裁决fix，仅一项：点月份时结束日期被截到今天。原负责人修复后集成5f2cee2，按桶实际起止日期下钻，允许的未来零值桶不再形成逆序。

新增同时覆盖9月完整边界与10月未来零值边界的浏览器回归，最终36/36。该新增检查首跑在图表异步重载期间访问空DOM导致35/36，增加加载等待所需的空值守卫后通过；未将脚本错误误报为产品缺陷。修复后类型与生产构建再次通过；原14状态截图按同名路径复捕，待同一reviewer裁决。

## 完成交付
2026-09-14约12:12完成本轮MVP。独立审查者对唯一桶边界修复评分resolved，disposition: ship；此verdict仅覆盖该修复项，不扩大为所有产品或生产验收通过。见 reading-insights-finish-verdict.md。审查者重新实际查看14张同路径重捕图，几何14/14；局部documenter已复核最终源码5f2cee2。

最终记录：交互36/36、原问答协议12/12、统计HTTP12/12、前端单元18/18，类型与生产构建通过。后端105项为负责人执行；真实PG、真实生成、生产每日入库仍待完成。主线程检测仅一次，保留既有Inter警告；未换风格或追加设计迭代。最新额度2026-09-14T04:10:18.183Z观测84%，未触及80%停止线。约78分钟为本轮墙钟时段，不是费用或净模型执行时间。
