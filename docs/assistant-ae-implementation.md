# 正式助手：A 布局 + E 时间线

2026-09-15，用户选定 A 的阅读侧栏布局与 E 的连续对话时间线。已落实到正式 `AssistantPanel.vue`，六套比较页继续保留为设计记录。

- 顶部放研究助手、历史、新建、收起；研究范围单独一行，待用事件紧随范围。
- 对话沿细线排列，用户空心节点、助手实心节点，正文开放排版，来源与详情在对应回答下展开。
- 底部独立输入区使用主题 rail / paper 层次；预览、按问题筛选、发送或停止保留。输入提示位于输入面之外。
- 保留字体 B、16 套主题、中英文、原流式逻辑、会话持久化、附件、引用、宽度调整及展开收起动画。
- 修正空闲时收起助手错误显示“已取消”的状态：仅取消正在生成的回答时显示。

验证：本地类型检查与生产构建通过，现有 42 项前端测试通过。正式组件使用 `?demo=1` 验证空态、发送、历史切换、引用展开，未调用付费模型。1440×1000 桌面与 390×844 手机截图已检查；320px 英文标题无横向溢出。燕麦陶红主题下阅读面、输入区底色、输入面分别使用 bg / rail / paper。长文本以临时 DOM 样本检查，未写入会话存储，滚动区可滚动且输入区底部等于 844px 视口底部。桌面历史展开后阅读列为 339px，标题不溢出。独立复核结论为 ship，无必须修复项。

截图位于本地 `.run/ae-empty-desktop.png`、`ae-answer-desktop.png`、`ae-answer-mobile.png`、`ae-english-long-mobile.png`、`ae-history-desktop.png`。长文本截图明确是布局样本，不作为模型答案质量验收。专用 Agent 开发仍为下一阶段。
