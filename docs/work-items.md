# 当前工作包与责任

所有任务使用独立 worktree，由 Astra 审查后集成。本文件记录已下发范围；验收通过情况以逐项报告为准。

| 工作包 | 负责人 | 目标 | 允许修改范围 | 必须遵守接口 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| 原型A | Sol | 编辑式首页同条件比较 | prototypes/sol、对应报告 | synthetic-ui-v1、设计基线 | 类型/构建、真实页面、共用浏览器断言 |
| 原型B | Terra | 研究工作台首页同条件比较 | prototypes/terra、对应报告 | 同原型A | 同原型A；比较后选定B |
| 后端首切片 | Sol | 精确列表、实体、日期、详情、Evidence与诚实状态 | backend、对应报告；迁移由Astra协调审查 | contracts/README.md、规格6–12/18–20节 | 精确ID/分页/错误/摘录断言、静态检查、离线迁移；不得假冒PG验收 |
| 正式四页 | Terra | 首页、详情、问答、受保护采集管理 | frontend、对应报告 | 新API与SSE样本、显式演示模式 | 请求取消/竞态、URL还原、引用映射、三尺寸浏览器、协议异常测试 |
| 集成验收 | Astra | 共用数据/契约、跨服务运行和独立验收 | 根配置、contracts、scripts、docs | 同上 | 实际HTTP与浏览器结果、保留失败证据、统一报告 |

首切片修复完成后继续按方案推进的工作包：

- 后端执行链路：持久任务、独立 scheduler/worker、租约与重试、RSS/原文版本、Evidence校验、调用账本和预算。数据库不可用时只验证纯逻辑/协议和迁移SQL，不声称任务抢占/事务通过。
- 检索扩展：确定性日期理解、中文预分词、PG全文、embedding profile与向量、RRF；真实模型凭据和真实PG就绪后再验收真实效果。
- 质量门槛：冻结历史gold、36题逐题断言、至少5个完整来源及100个真实标准事件、真实问答引用。缺少外部材料的项保持待验收。

已知实施边界：普通列表使用与条件绑定的稳定游标及单页一致事务；跨页严格内容快照尚待QueryRun/版本快照实现，不能宣传为动态写入下的严格集合冻结。

用户确认：Pro仅周额度，初始95%，到80%即停止。当前无数据库与历史gold，先推进工程。付费模型没有配置和预算，不执行付费调用。
