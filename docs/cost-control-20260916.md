# 2026-09-16 费用控制调整

用户要求优先“能用、少花钱”，避免重复无效验证。此决定优先于旧方案中默认的大规模质量评测安排；旧文档保留历史，不自动执行四十题双路径收费评测。常规开发使用零费用夹具，仅验证受影响功能。模型真实兼容已有通过记录时，不为无关改动重复调用。

## 已在线生效

- 同一 IP 滚动 48 小时最多 **5 次提问**，追问也计入。既有账本不清空，不重新发放额度。
- 隐藏研究助手底部常驻的剩余额度说明；额度用完时仍提供中英文错误提示。
- 线上最小补丁为 `e461e97`，仅六个额度/UI/现有测试文件。API 已重启，会话接口确认 `limit=5, window_hours=48`。前端保持运行，生产构建通过。
- 用户明确要求暂停付费事件提取。`ai-radar-history-extract.timer` 与 `ai-radar-worker.service` 已停止并 disabled，提取 service inactive。浏览、统计及小助手保留。采集任务暂不消费，没有删除既有队列或数据。
- 付费提取不会由后续常规服务恢复步骤自动重启，需先排查失败原因并按用户意愿恢复。

## 今日项目账本（截至 14:14，Asia/Shanghai）

只读查询 2026-09-16 当日已结束的 DeepSeek 调用，未读取用户正文或密钥。

| 用途/结果 | 次数 | 已知输入 tokens | 已知输出 tokens |
| --- | ---: | ---: | ---: |
| 后台受限兼容测试，完成 | 2 | 3,994 | 227 |
| 事件提取，结构/发布校验失败 | 1,472 | 1,284,610 | 387,900 |
| 事件提取，筛除 | 2,590 | 2,255,719 | 243,731 |
| 事件提取，传输结果未知 | 5 | 未知 | 未知 |

兼容测试使用此前用户批准的单次验证，检查 pi 工具调用、分类聚合、DeepSeek 流式协议、usage 与数字引用。它只发送规则、测试问题和分类计数，没有事件正文或用户会话；没有自动重试。详情见 [P2 记录](research-agent-p2-progress.md)。

自动事件提取共 4,067 条记录，是今日主要用量；已知输入 3,540,329、输出 631,631 tokens。失败码为 `invalid_extraction`，意味着结构解析或可发布性检查未通过，现有统一错误码不足以确认更具体的原因。被筛除不等于请求失败，也不代表没有 token 消耗。五条未知用量不能按零计。

账本没有实际金额，本文不估算费用；以供应商账单为准。本记录仅覆盖本项目，不代表同一 API 账户的其他应用。

## 本次有限验证

本地：现有后端额度/策略检查 36 项、前端额度检查 2 项、Node 策略检查 2 项通过，前端构建通过。Ubuntu 只执行两项随机隔离数据库测试：五次后拒绝第六次、滚动窗口与扣额前拒绝；均通过。线上只读取会话额度确认生效，没有发送测试问题。

本次没有付费模型调用，没有业务库迁移，没有启用专用 Agent 或公开 Python。其余 Agent 候选变更仍未随本次小补丁上线。
