# AI 革新雷达

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

用户已确认同时服务个人与公众读者：

- 项目发起人个人使用：跟踪 AI 进展，按日期、分类和实体回查事件，核对来源原文。
- 公众读者：浏览 AI 事件、按条件查找资料并提问，通过引用检查事实依据。

用户已确认首页优先让个人与公众读者看懂 AI 动态，再进入筛选、问答和证据核查；保留四页功能。采集管理属于维护工作，不是公众阅读的必经步骤。

## Product Purpose

提供中文 AI 事件浏览、检索与研究问答工具。核心目标是完整定位当前库内符合明确条件的可见事件，并把回答中的事实对应到可核查的来源段落。

成功意味着：用户知道找到了哪些事件、依据是什么、总结覆盖了多少，以及哪些部分仍不确定。库内完整匹配不等于覆盖全网；检索完整也不等于回答叙述了所有结果。

## Positioning

以事件为组织单位，将多篇来源报道关联到事件；结构化条件优先，原文版本与段落证据可追溯。开放语义检索和模型总结应服务于这一机制。

这是产品方向和约束，不是已经通过真实数据验收的市场优势声明。

## Operating Context

- 中文 Web 界面，支持桌面、平板和手机浏览器。当前四条主要路径为事件首页、事件详情、问答和采集管理。
- 首页支持全库搜索、日期/分类过滤、匹配总数和分页；详情承载实体、来源与保存的段落摘录。
- 问答承载问题、显式条件、已解析范围、结果及引用，需清楚显示覆盖范围与失败原因。
- 维护者通过受保护的管理入口查看来源健康与运行状态、触发采集；管理凭据不应进入 URL 或浏览器持久化存储。
- 现有方案采用单部署范围，不引入计费 SaaS、自训模型或原生移动端。面向公众的具体部署、访问控制和服务保障尚未决定，不能宣称已经公开上线。
- 现有工程为 Vue 3 / TypeScript / Vite 前端与 FastAPI / SQLAlchemy 后端；数据库目标为 PostgreSQL 16 + pgvector。API、调度器与 worker 分进程运行。

## Capabilities and Constraints

### 已实现的工程基础

四页、显式合成数据模式、精确列表与分页、详情及段落引用、确定性日期/实体查询计划、持久采集任务与来源管理工程已落码。前端能够处理流式协议、取消和错误；这些客户端流式行为使用明确标注的模拟流验收。

### 尚未完成或未真实验收

真实 PostgreSQL 迁移、并发事务与恢复尚未验收；模型提取、标准事件发布、全文/向量混合检索、完整集合版本快照、真实生成问答与实际费用结算仍有实施工作。配置模型密钥本身不会使未实现能力可用。

本地生产抓取曾因 DNS 返回非公网地址而被阻断。历史漏检事件及 DeepSeek 样本尚无真实冻结材料，用户已同意先做合成结构回归。当前工程检查通过不代表完整 P0 通过。

详细状态以 [验收矩阵](docs/acceptance-matrix.md) 和 [后续验收顺序](docs/next-acceptance.md) 为准，避免把阶段性数字固化为产品承诺。

### 必须保留的语义

- 标准事件与来源报道分离；同一事件的多源报道不能重复计为多个事件。
- 明确日期、分类、实体约束的完整结果不能由向量 top-k 决定。
- Evidence 定位不可变原文版本中的段落；链接可访问、摘录存在、摘录支持主张是不同的检查。
- 日期区间起止均包含，显示适用时区；实体别名、同时匹配与任一匹配不能混淆。
- 无资料、数据库故障、未配置模型、预算不足、取消与中断必须区别呈现。
- 实际、估算和未知费用分别记录；未知不能填为零。
- 演示数据、模拟回答与真实采集结果必须明确区分，普通接口失败不能自动改为模拟成功。

## Brand Commitments

项目现有名称为“AI 革新雷达”，主要内容和操作语言为中文。文案应准确表达事件、证据、范围与限制，不添加未经核实的新闻、客户、效果数据或服务承诺。

用户已确认以桌面阅读空间为主体，以云层切面的层次和动作谱的日期节奏辅助阅读。具体实现规则见 [当前设计系统](DESIGN.md)；[早期设计基线](docs/design-requirements.md) 保留为历史记录。

## Evidence on Hand

- 原始实施规格：../AI-Radar-Implementation-Spec-v1.0.md；本次安排：简单安排.txt。
- [设计基线](docs/design-requirements.md)、[原型比较](docs/prototype-comparison.md) 和 frontend/ 提供既有界面与交互证据。
- contracts/prototype-events.json、contracts/regression/、contracts/sse/ 为合成数据、结构回归和协议样本，不是真实新闻或历史 gold。
- docs/ 下的浏览器、API、回归和来源探测 JSON 记录执行层与限制；截图位于本地 artifacts/browser/。
- 尚无可用于历史真实验收的原站 ID/冻结材料，也没有完成真实模型质量、完整来源链路或公开部署验收的证据。

## Product Principles

1. 明确条件下优先保证库内检索完整性，并准确报告范围。
2. 事实可追溯到保存的来源段落，摘要不能替代证据。
3. 展示能力边界与不确定性，失败不能伪装为无资料或成功。
4. 个人与公众都能完成浏览、回查和核对；维护操作与阅读任务分开。
5. 先验证再作承诺，真实链路与合成回归分别验收。

## Accessibility & Inclusion

现有工程基线要求清晰键盘焦点、可键盘展开摘录、至少 44px 触摸目标、状态反馈及不单靠颜色传意；在 390/768/1440px 下检查布局。后续工作应保留这些能力。尚未约定正式 WCAG 合规等级或特定辅助技术验收标准，不宣称已经取得合规认证。
