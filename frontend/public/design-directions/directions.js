const $=s=>document.querySelector(s);
const icons={radar:'<circle cx="12" cy="12" r="9"/><circle cx="12" cy="12" r="5"/><path d="m12 12 7-7"/><circle cx="12" cy="12" r="1"/>',chat:'<path d="M21 15a4 4 0 0 1-4 4H7l-4 3V7a4 4 0 0 1 4-4h10a4 4 0 0 1 4 4zM8 9h8M8 13h5"/>',arrow:'<path d="M5 12h14m-5-5 5 5-5 5"/>',close:'<path d="m6 6 12 12M6 18 18 6"/>',search:'<circle cx="10" cy="10" r="6"/><path d="m15 15 5 5"/>',source:'<path d="M10 5H5v14h14v-5M13 3h8v8m0-8L10 14"/>',check:'<path d="m5 12 4 4L19 6"/>',lock:'<rect x="5" y="10" width="14" height="11" rx="2"/><path d="M8 10V7a4 4 0 0 1 8 0v3"/>'};
const svg=(name)=>`<svg viewBox="0 0 24 24" aria-hidden="true">${icons[name]||icons.radar}</svg>`;
const dirs=[
 {id:'a',name:'阅读目录',tag:'按主题找到变化',thesis:'把首页变成一份可以快速翻阅的公共目录。上方导航，左侧主题索引，右侧连续阅读。',risk:'比资讯门户更克制，第一眼的视觉冲击较弱。',type:'目录式无衬线 / 标题 42 / 正文 16',layout:'顶部导航 + 主题索引 + 单列事件',stats:'图表与结果并排，点击分类直接缩小结果。',admin:'来源以紧凑列表排列，配置在右侧展开。'},
 {id:'b',name:'研究刊物',tag:'读懂，而不只是看见',thesis:'借用研究刊物的编排：宋体标题、日期边注、正文宽度固定，让每条变化值得读完。',risk:'长文阅读舒适；同屏事件数少于电讯方案。',type:'Noto Serif SC 标题 46 / 正文 17',layout:'刊头 + 日期边注 + 长文阅读栏',stats:'像一份简报，先结论，再图表，再可核查清单。',admin:'独立工作台，保留相同纸色但使用紧凑无衬线。'},
 {id:'c',name:'即时电讯',tag:'快速扫描每一次更新',thesis:'把日期、分类、标题、来源对齐成清晰的电讯行；高频读者可以在一个屏幕内找到更多信息。',risk:'信息密度较高，新访客需要更明确的空状态引导。',type:'无衬线标题 32 / 正文 15 / 数字等宽',layout:'窄侧栏 + 工具条 + 时间顺序行',stats:'紧凑总数带、趋势和可排序结果，减少无效留白。',admin:'表格工作台，批量操作固定在列表上方。'},
 {id:'d',name:'编辑选刊',tag:'重点先被看见',thesis:'首页用一条重点事件和一列短讯组成不对称开篇，下方按专题展开。色彩只给真正的重点。',risk:'需要可解释的重点排序；预览中的首条仅为合成示例。',type:'粗标题 52 / 正文 16 / 大小对照',layout:'不对称重点区 + 短讯列 + 专题网格',stats:'大幅趋势与分类排行，匹配事件在下方展开。',admin:'批次状态优先，再进入来源和模型设置。'},
 {id:'e',name:'数据图鉴',tag:'从变化回到证据',thesis:'首页仍然是事件，右侧加入当前结果的分类目录。统计页把分布、时间和清单连起来看。',risk:'工具感更强，需控制辅助数字数量。',type:'无衬线标题 38 / 正文 16 / 数据 28',layout:'左侧导航 + 事件阅读区 + 分类侧注',stats:'图表主区与结果侧栏联动，助手独立并排。',admin:'来源状态、任务运行和模型用量采用同一数据语言。'},
 {id:'f',name:'公共导览',tag:'一眼找到你关心的方向',thesis:'以公共导览牌为灵感，明确的色带、编号和大标题引导普通读者。复杂信息留在下一层。',risk:'品牌个性最强，黄色应限于导航和重点提示。',type:'醒目黑体标题 48 / 正文 17 / 强编号',layout:'导览色带 + 大分类入口 + 编号事件',stats:'用三个直白问题组织：多少、哪类、哪一天。',admin:'用“来源 / 采集 / 模型”三条任务路径组织后台。'}
];
const categories=['模型发布','智能体工具','框架与 SDK','研究','产品','产业'];
const titles=['新一代推理模型公布评测方法与适用范围','工作流工具加入可追溯的执行记录','开放框架简化多模型服务的接入','研究团队提出长文本证据定位方法','协作产品增加文档问答与引用核验','开发者生态发布新的互操作规范','推理模型更新公开测试结果','智能体工具支持任务回放与人工确认','框架优化批量推理的资源调度','研究比较不同检索策略的覆盖情况','知识产品改进来源展示与阅读体验','产业观察：开放标准与工具协作'];
const summaries=['这条合成示例展示如何呈现模型能力、使用范围和可核查的来源。读者可以打开事件，再查看支持该摘要的证据。','把执行过程变成可回看的记录，让用户知道任务经过了哪些步骤，并在需要时进行人工确认。','示例摘要围绕开发者最关心的接入方式、兼容范围和部署条件组织，不把宣传语直接当作结论。','这条示例研究关注一个具体问题：较长的回答能否准确回到原文位置，并让读者自己核对。'];
const events=titles.map((title,i)=>({id:i,title,summary:summaries[i%4],category:categories[i%6],day:15-Math.floor(i/4),source:['示例研究团队','示例开发者博客','示例项目公告'][i%3],sources:1+i%3}));
let direction=new URLSearchParams(location.search).get('scheme')||'a',page='home',category='全部',query='',days=7,activeEvent=0,assistantMessages=[],busy=false;
if(!dirs.some(d=>d.id===direction))direction='a';
const filtered=()=>events.filter(e=>(category==='全部'||e.category===category)&&(!query||e.title.includes(query)||e.summary.includes(query))&&(days!==1||e.day===15));
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function nav(){return `<a class="identity" href="#" data-page="home">${svg('radar')}<span>AI 革新雷达</span></a><nav aria-label="站内导航">${[['home','AI 动态'],['stats','统计与问答'],['admin','管理员后台']].map(([id,name])=>`<button data-page="${id}" aria-current="${page===id?'page':'false'}">${name}</button>`).join('')}</nav><span class="edition">设计预览 · 合成示例</span>`}
function filters(){return `<section class="filters" aria-label="筛选"><label class="search">${svg('search')}<input id="search" placeholder="搜索标题、摘要" value="${esc(query)}" aria-label="搜索事件"></label><div class="category-tabs">${['全部',...categories].map(c=>`<button data-category="${c}" aria-pressed="${category===c}">${c}</button>`).join('')}</div><div class="period">${[[1,'今天'],[7,'近 7 天'],[30,'近 30 天']].map(([n,t])=>`<button data-days="${n}" aria-pressed="${days===n}">${t}</button>`).join('')}<span>2026 年 9 月</span></div></section>`}
function eventRow(e,i){return `<article class="event"><div class="event-index">${String(i+1).padStart(2,'0')}<span>9 月 ${e.day} 日</span></div><div class="event-content"><div class="eyebrow"><span>${e.category}</span><span>${e.source}</span></div><h2><button data-event="${e.id}">${e.title}${svg('arrow')}</button></h2><p>${e.summary}</p><footer><span>${e.sources} 个来源</span><button data-event="${e.id}">查看事件与证据 ${svg('arrow')}</button></footer></div></article>`}
function categoryIndex(){return `<aside class="topic-index"><span class="kicker">主题目录</span>${categories.map((c,i)=>`<button data-category="${c}" aria-pressed="${category===c}"><span class="topic-no">0${i+1}</span>${c}<span class="topic-count">${events.filter(e=>e.category===c).length}</span></button>`).join('')}<p>每一条变化，都能回到来源。</p></aside>`}
function home(){const rows=filtered();return `<header class="page-heading"><div><h1>AI 动态<span class="title-dot">.</span></h1></div><p class="intro">从事件流开始，<br>再回查来源与证据。</p><div class="issue-number">09<span>2026 / SEPTEMBER</span></div></header>${filters()}<div class="home-layout">${categoryIndex()}<section class="event-feed" aria-label="事件列表"><div class="feed-label"><span>${rows.length} 条匹配事件</span><span>按发布时间排序</span></div>${rows.length?rows.map(eventRow).join(''):'<p class="empty">没有匹配事件。试试其他关键词或分类。</p>'}</section><aside class="margin-note"><span class="kicker">阅读线索</span><h3>先看变化，<br>再看依据。</h3><p>打开任一事件可以查看来源，并把它带入研究助手。</p><span class="sample-stamp">12 条合成示例<br>6 个分类</span></aside></div>`}
function stats(){const rows=filtered(),counts=categories.map(c=>rows.filter(e=>e.category===c).length),max=Math.max(1,...counts);return `<header class="page-heading"><div><h1>统计与问答</h1></div><p class="intro">先看整体，<br>再问一个具体问题。</p></header>${filters()}<div class="metrics"><div><span>这个范围有多少事件</span><strong>${rows.length}<small> 条</small></strong></div><div><span>覆盖哪些分类</span><strong>${counts.filter(Boolean).length}<small> 类</small></strong></div><div><span>累计来源关联</span><strong>${rows.reduce((s,e)=>s+e.sources,0)}<small> 个</small></strong></div></div><div class="stats-layout"><section class="chart"><p class="kicker">01 / 哪类更新更多</p><h2>分类分布</h2><div class="bars">${categories.map((c,i)=>`<button data-category="${category===c?'全部':c}" title="筛选${c}"><span>${c}</span><i style="--amount:${counts[i]/max*100}%"></i><b>${counts[i]}</b></button>`).join('')}</div><p class="chart-note">点击分类可筛选，统计始终来自当前范围。</p></section><section class="chart"><p class="kicker">02 / 哪一天有变化</p><h2>日期与分类</h2><div class="heatmap"><span></span>${[13,14,15].map(d=>`<span>${d} 日</span>`).join('')}${categories.map(c=>`<strong>${c}</strong>${[13,14,15].map(d=>{let n=rows.filter(e=>e.day===d&&e.category===c).length;return `<button data-cell="${c}|${d}" style="--heat:${n?'.65':'.04'}" title="9月${d}日 ${c}：${n} 条">${n}</button>`}).join('')}`).join('')}</div><p class="chart-note">浅色表示少，深红表示多。数字同时保留。</p></section><section class="stat-results"><div class="feed-label"><h2>匹配事件</h2><button data-assistant>询问这个范围 ${svg('chat')}</button></div>${rows.map(eventRow).join('')||'<p class="empty">当前范围没有事件。</p>'}</section></div>`}
function admin(){return `<header class="page-heading"><div><h1>管理员后台</h1></div><p class="intro">清楚地知道，<br>每一条数据从哪里来。</p></header><div class="admin-notice">${svg('lock')}后台布局预览：以下操作只改变本页示例，不连接真实采集或模型服务。</div><nav class="admin-tabs" aria-label="后台栏目">${['来源管理','采集记录','模型设置'].map((t,i)=>`<button data-admin-tab="${i}" aria-pressed="${i===0}">${t}</button>`).join('')}</nav><section id="admin-content">${sourceAdmin()}</section>`}
function sourceAdmin(){return `<div class="section-heading"><div><h2>信息来源</h2><p>先探测，再启用采集。</p></div><button class="solid" id="probe">批量探测</button></div><div class="source-table">${['示例研究团队','示例项目公告','示例开发者博客','示例开放论文库'].map((n,i)=>`<article><span class="source-initial">0${i+1}</span><div><h3>${n}</h3><p>RSS / 官方公告 · 每日检查</p></div><span class="source-state">${i===3?'待探测':'可访问'}</span><button data-source-probe>探测</button><button data-enable aria-pressed="true">已启用</button></article>`).join('')}</div><p id="probe-status" role="status"></p>`}
function records(){return `<div class="section-heading"><h2>采集记录</h2><span>合成批次</span></div><article class="run"><strong>部分完成</strong><span>9 月 15 日 · 08:00</span><span>发现 12 / 发布 9</span></article><div class="error-summary">3 篇文章暂时无法读取。已完成的内容可以正常浏览。</div><details><summary>查看原始日志</summary><pre>示例日志：HTTP 403 · 远端拒绝访问\n下一步：检查来源可用性；稍后重试。</pre></details>`}
function model(){return `<div class="section-heading"><h2>模型设置</h2><span>仅预览，不发送请求</span></div><form id="model-form"><label>服务提供商<select id="provider"><option>DeepSeek</option><option>OpenAI 兼容接口</option><option>自定义</option></select></label><label>接口地址<input id="base-url" value="https://api.deepseek.com"></label><label>模型名称<input value="deepseek-flash"></label><label>API 密钥<input type="password" placeholder="留空保留原配置" autocomplete="off"></label><button class="solid">保存预览设置</button><span id="model-status" role="status"></span></form><section class="test-conversation"><h3>回答测试</h3><p><b>发送</b> 请简要说明你能做什么。</p><p><b>返回示例</b> 我可以根据提供的资料回答问题，并保留引用供你核查。</p></section>`}
function rationale(){const d=dirs.find(d=>d.id===direction);$('#rationale').innerHTML=`<div><span class="kicker">方案 ${d.id.toUpperCase()} / ${d.name}</span><h2>${d.tag}</h2><p>${d.thesis}</p></div><dl><dt>首页</dt><dd>${d.layout}</dd><dt>统计</dt><dd>${d.stats}</dd><dt>后台</dt><dd>${d.admin}</dd><dt>字体</dt><dd>${d.type}</dd><dt>取舍</dt><dd>${d.risk}</dd></dl><p>六套均保留事件首页、独立统计页、证据抽屉与并排助手。这里只预览，不会覆盖当前产品。也可以保留现有布局，仅采用已完成的动效和图标。</p>`}
function render(){document.body.dataset.direction=direction;document.body.dataset.page=page;$('#directions').innerHTML=dirs.map(d=>`<button data-direction="${d.id}" aria-pressed="${direction===d.id}"><b>${d.id.toUpperCase()}</b><span>${d.name}</span></button>`).join('');$('#app').innerHTML=`<div class="site-shell"><header class="site-nav">${nav()}</header><main>${page==='home'?home():page==='stats'?stats():admin()}</main><footer class="site-footer"><span>AI 革新雷达</span><span>设计预览 · 全部内容为合成示例</span><button data-assistant>研究助手 ${svg('chat')}</button></footer></div>`;rationale();$('#launcher').innerHTML=svg('chat');}
function openEvent(id){activeEvent=Number(id);const e=events[activeEvent];$('#event-dialog').innerHTML=`<header><button data-close-event aria-label="返回事件列表">${svg('arrow')}返回</button><span>事件 / ${e.category}</span></header><div class="detail-body"><p class="kicker">合成示例 · 2026 年 9 月 ${e.day} 日</p><h1 id="event-title">${e.title}</h1><p class="detail-summary">${e.summary}</p><button class="solid" data-attach>${svg('chat')}加入研究对话</button><section><p class="kicker">来源与证据</p><h2>${e.source}</h2><blockquote>这是用于比较阅读版式的合成段落，不是已保存的新闻原文。实际产品的证据将关联不可变版本与段落编号。</blockquote><details><summary>来源信息</summary><p>示例版本 preview-v1 · 段落 preview-p01</p></details></section></div>`;$('#event-dialog').showModal()}
function openAssistant(attachment){$('#event-dialog').close();$('#assistant').hidden=false;$('#launcher').hidden=true;$('#assistant').innerHTML=`<header><strong>${svg('chat')}研究助手</strong><button data-close-assistant aria-label="收起研究助手">${svg('close')}</button></header><p class="assistant-scope">${attachment?'依据事件：'+esc(attachment):'当前筛选范围'} · 合成示例</p><div class="conversation">${assistantMessages.join('')||'<div class="assistant-welcome"><h2>你想进一步<br>了解什么？</h2><p>回答与引用在这里展开。对话输入始终靠近阅读位置。</p></div>'}</div><form id="ask-form"><label for="question">问题</label><textarea id="question" rows="3" placeholder="例如：这些变化有哪些共同点？"></textarea><footer><span>Enter 发送 · Ctrl+Enter 换行</span><button class="solid">发送 ${svg('arrow')}</button></footer></form>`;$('#question').focus()}
document.addEventListener('click',e=>{const b=e.target.closest('button,a');if(!b)return;if(b.dataset.direction){direction=b.dataset.direction;history.replaceState(null,'','?scheme='+direction);render()}if(b.dataset.page){e.preventDefault();page=b.dataset.page;render();scrollTo(0,0)}if(b.dataset.category){category=b.dataset.category;render()}if(b.dataset.days){days=Number(b.dataset.days);render()}if(b.dataset.event!==undefined)openEvent(b.dataset.event);if(b.hasAttribute('data-close-event'))$('#event-dialog').close();if(b.hasAttribute('data-assistant')||b.id==='launcher')openAssistant();if(b.hasAttribute('data-attach'))openAssistant(events[activeEvent].title);if(b.hasAttribute('data-close-assistant')){$('#assistant').hidden=true;$('#launcher').hidden=false;$('#launcher').focus()}if(b.dataset.cell){const[c,d]=b.dataset.cell.split('|');category=c;render();const first=events.find(x=>x.category===c&&x.day===Number(d));if(first)openEvent(first.id)}if(b.dataset.adminTab){document.querySelectorAll('[data-admin-tab]').forEach(t=>t.setAttribute('aria-pressed',String(t===b)));$('#admin-content').innerHTML=[sourceAdmin,records,model][Number(b.dataset.adminTab)]()}if(b.hasAttribute('data-enable')){const on=b.getAttribute('aria-pressed')==='true';b.setAttribute('aria-pressed',String(!on));b.textContent=on?'已停用':'已启用'}if(b.id==='probe'||b.hasAttribute('data-source-probe')){if(busy)return;busy=true;b.disabled=true;b.textContent='探测中…';let n=0;const total=b.id==='probe'?4:1;const timer=setInterval(()=>{$('#probe-status').textContent=`已完成 ${++n} / ${total} · 预览探测`;if(n===total){clearInterval(timer);busy=false;b.disabled=false;b.textContent=total===4?'批量探测':'探测';document.querySelectorAll('.source-state').forEach(x=>x.textContent='可访问')}},400)}});
document.addEventListener('input',e=>{if(e.target.id==='search'){const caret=e.target.selectionStart;query=e.target.value;render();$('#search').focus();$('#search').setSelectionRange(caret,caret)}});
document.addEventListener('submit',e=>{e.preventDefault();if(e.target.id==='model-form')$('#model-status').textContent='预览配置已保存，未连接服务。';if(e.target.id==='ask-form'){const q=$('#question').value.trim();if(!q)return;assistantMessages.push(`<article class="user-message"><span>你</span><p>${esc(q)}</p></article><article><span>助手 · 预览回答</span><p>这组示例覆盖模型、工具与研究。可以先比较分类数量，再打开事件核对来源。正式回答将使用所选范围内的真实资料。</p><button data-event="${activeEvent}">查看相关事件 ${svg('source')}</button></article>`);openAssistant();$('.conversation').lastElementChild?.scrollIntoView({block:'end',behavior:'smooth'})}});
document.addEventListener('keydown',e=>{if(e.target.id==='question'&&e.key==='Enter'&&!e.ctrlKey&&!e.isComposing){e.preventDefault();$('#ask-form').requestSubmit()}if(e.key==='Escape'&&!$('#event-dialog').open&&!$('#assistant').hidden){$('#assistant').hidden=true;$('#launcher').hidden=false}});
$('#explain').onclick=()=>{$('#rationale').hidden=!$('#rationale').hidden;$('#explain').setAttribute('aria-expanded',String(!$('#rationale').hidden))};
$('#event-dialog').onclick=e=>{if(e.target===$('#event-dialog')){const r=e.target.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)e.target.close()}};
render();
