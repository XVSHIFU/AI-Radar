# 新版 SSE 验收样本

这些文件是本项目明确制定的合成协议样本，不是原站抓包。

normal 包含注释心跳、多行 data、中文和非连续引用编号2。客户端须按 index 映射，不得将数组下标+1当编号。empty 是正常无资料；failed 是服务故障；truncated 必须报告不完整。

解析器应在任意字节边界拆分这些样本（包括中文多字节中间）、转换CRLF并验证；用户取消不应触发自动重发计费请求。缺失done、重复done、sources后token属于不完整或协议错误，不能显示完成。
